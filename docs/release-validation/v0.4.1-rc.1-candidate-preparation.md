# `v0.4.1-rc.1` Candidate Preparation — I3.2

Spec: [`docs/specifications/0.4.1/i3-hardening-qualification-and-release.md`](../specifications/0.4.1/i3-hardening-qualification-and-release.md)
§3 (Entry Conditions), §20 (Package and Producer Version), §21 (Candidate Identity and Freeze),
§22-24 (Existing Evaluation Regression, Benchmark Candidate Evidence, Clean-Checkout Source
Qualification), §25 (CI/Security/Dependency Gates), §35 (I3.2).

## Entry audit (spec §3)

Every I3 entry condition, independently re-verified against evidence rather than assumed from a
prior textual claim, against the merged `main` tip at candidate-freeze time
(`bc03602f95557eb0450506a9f5c3a7f7c296b2da`).

| Condition | Result | Evidence |
|---|---|---|
| I1 GO | ✓ | `docs/specifications/0.4.1/README.md`'s I1 status entry / `i1-completion-record.md`; candidate `385604b7c7098184b04193abf13727e90cd8b555` |
| I2 GO | ✓ | `docs/specifications/0.4.1/README.md`'s I2 status entry / `i2-completion-record.md`; candidate `d37399b256062d4da5c116ae332ed25d69395e04` |
| One declared-versus-observed semantic owner; qualification/coverage mismatches = 0 | ✓ | `tests/integration/test_qualification_consistency.py` (part of the clean-checkout integration run below) |
| Both messaging guards production-reachable; refusal creates zero semantic artifacts | ✓ | `tests/unit/test_messaging_guards.py`, `tests/unit/test_adapter.py`, `tests/integration/test_adapter.py` (part of the clean-checkout runs below) |
| Valid declared Queue and explicit runtime-only Service behavior preserved | ✓ | Same suites as above |
| Exactly three read-only MCP tools, `schema_version = 0.4` | ✓ | `tests/unit/test_mcp_discovery.py`; independently re-confirmed live during the hero-demo rerun below (`tools/list` → `get_architecture_drift`, `get_evidence`, `get_service_dependencies`) |
| No Topic/Subscription family; messaging operation recognition frozen | ✓ | `tests/unit/test_adapter.py`'s frozen Quarkus/Airflow exact-shape regressions (part of the clean-checkout unit run) |
| CI/security/dependency checks for the merged I2 state = PASS | ✓ | Re-verified at I3.2 entry against `main` tip `795653e` (I3.1's merge) before I3.2 work began: `lint + test`, `CodeQL` ×2, `pip-audit` — all `success` |
| I1/I2 blockers = 0 | ✓ | `i1-completion-record.md`/`i2-completion-record.md`'s own exit statements: "release blockers = 0" |

No entry condition failed. `NO-GO` was not triggered; I3.2 candidate preparation proceeded.

## Candidate identity and freeze (spec §21)

Per spec §21, `RELEASE_CANDIDATE_SHA` can only be the exact commit produced once the candidate-
preparation PR is merged (this repository squash-merges, so the final SHA is not knowable before
merge) — I3.2's own PR (#120) therefore could not itself cite the frozen candidate; this record is
the immediate evidence-only follow-up, bound to the resulting merge commit:

```text
RELEASE_CANDIDATE_SHA = bc03602f95557eb0450506a9f5c3a7f7c296b2da
```

Verified against that exact commit via `gh api repos/.../commits/bc03602f.../check-runs` (never
`gh pr checks` or the PR's ambient current-head view): `lint + test`, `CodeQL` (`analyze (actions)`,
`analyze (python)`), `dependency security scan (pip-audit, spec §25)` — all `completed`/`success`.
(`update-uv-graph`, GitHub's native dependency-graph submission workflow, also `completed`/
`success` — not one of spec §25's named required checks, recorded here for completeness only.)

No commit landed on `main` between I3.2's merge and this record — `RELEASE_CANDIDATE_SHA` is the
literal `main` tip this record was produced against; no candidate-changing correction was required.

## Version and package metadata (spec §20)

Already consistent as of `bc03602` — no fix needed at this step (I3.2 itself performed and verified
this bump, per PR #120):

```text
pyproject.toml                                    version = "0.4.1"
uv.lock (root project entry)                       version = "0.4.1"
app.mcp.server.mcp_server.version                  = "0.4.1"   (via app.version.package_version())
app.mcp.wiring's production-wired Producer.version = "0.4.1"   (via app.version.package_version())
evaluation.architecture_answers.runner's Producer.version = "0.4.1"  (via app.version.package_version())
```

`schema_version` remains `"0.4"` throughout — confirmed by the schema freeze audit below and by
every evaluation-scenario/hero-demo answer produced in this record.

## Schema freeze audit

Committed schema hashes, unchanged from `v0.4.0`/I1-I2 — no public schema content changed for
`v0.4.1`:

```text
sha256:94af46cd4194bcf037e7aed17135cf41fc786b8ca75fc79042ef715b4dd6a0bd  architecture-answer.schema.json
sha256:d81f1ab0f5b8363c689f6a8cc0cc9d06a01a6c7836ac7cdbd821d8816b3be69c  drift-answer.schema.json
sha256:08cd5a6473aba3086caef16ae9006c3399b05de59cc2b5f0716f774ebd816503  evidence-answer.schema.json
```

`tests/unit/test_architecture_intelligence_schema_frozen.py` (part of the clean-checkout unit run
below) confirms committed schemas equal generated schemas.

## Clean-checkout source qualification (spec §24)

Run from a freshly created `git worktree` at the exact candidate SHA
(`bc03602f95557eb0450506a9f5c3a7f7c296b2da`, verified `git status --porcelain` empty before
starting) — never the developer's mutable working tree.

```bash
uv sync --locked                        -> Resolved 65 packages, no lock drift
uv run ruff check .                     -> All checks passed
uv run ruff format --check .            -> 249 files already formatted
uv run pytest tests/unit -q             -> 985 passed
uv run pytest tests/integration -q      -> 257 passed
uv run --with pip-audit pip-audit       -> No known vulnerabilities found
```

### Candidate-bound architecture-answers evaluation (spec §22 item 6-7)

```bash
uv run python -m evaluation answers --candidate-sha bc03602f95557eb0450506a9f5c3a7f7c296b2da
```

```text
result:                        PASS
scenarios:                     23
passed:                        23
failed:                        0
run_count:                     2
semantic_outputs_identical:    true
real_system_qualification:     quarkus-super-heroes PASS, apache-airflow PASS
cross_tool_invariants:         dependency_to_drift PASS, drift_to_evidence PASS
```

The committed `evaluation/architecture_answers/results/architecture-answers-evaluation-result.json`
artifact was **not** updated by this record — spec §22 requires the exact-candidate re-run as
regression evidence, not that the repository's own committed baseline (still correctly bound to
`v0.4.0`'s GO candidate) track every intermediate patch-release candidate. It will be rebound once
`v0.4.1` itself is the qualified state, at I3.3/I3.4, matching how `v0.4.0`'s own I4.1 handled this
distinction.

### Independent MCP client golden path (spec §22 item 8)

Covered by `tests/integration/test_mcp_independent_client_golden_path.py`, part of the clean-
checkout integration run above — the real production app served over real network HTTP, driven by
a client importing nothing from `app.*`.

### Tool discovery (spec §22 item 9)

Covered by `tests/unit/test_mcp_discovery.py` above, and independently re-confirmed live during the
hero-demo rerun below.

### Deterministic hero path (spec §22 item 10)

Run twice from separate clean `docker compose -f docker-compose.demo.yml down -v` states, from the
same clean worktree, per `examples/runtime-demo/hero-demo.md`:

```text
tools/list                     -> get_architecture_drift, get_evidence, get_service_dependencies
get_architecture_drift         -> LegacyPricingService OBSERVED_ONLY (hero finding)
                                   unused-q NOT_OBSERVED_IN_WINDOW
                                   ProductService/payment-q correctly excluded (CONFIRMED)
get_evidence (same snapshot)   -> 3/3 records resolved, 0 missing
```

Both runs' full `structuredContent` for `get_architecture_drift` diffed byte-for-byte identical
(including `snapshot_id`/`model_revision`) via independent Python comparison, not visual inspection.
`producer.version = "0.4.1"`; `producer.build_revision = "unknown"` in this local
docker-compose-built image, as expected — this image never sets `AIP_BUILD_REVISION` (that's the
release-triggered `docker.yml` workflow's job, verified against the actual published RC image at
I3.3, not this local demo build).

### Schema-generation/frozen-schema checks (spec §22 item 11)

Covered by `test_architecture_intelligence_schema_frozen.py`, part of the clean-checkout unit run
above.

### Benchmark smoke profile, twice from clean state (spec §22 item 12)

```bash
uv run python -m benchmarks --profile smoke --candidate-sha bc03602f95557eb0450506a9f5c3a7f7c296b2da --out smoke-run-1.json
uv run python -m benchmarks --profile smoke --candidate-sha bc03602f95557eb0450506a9f5c3a7f7c296b2da --out smoke-run-2.json
```

Both runs exit `0`; every scale point `structural_validation=PASS`/`semantic_validation=PASS`.
`benchmarks.snapshot_read_cost.canonicalize_for_determinism()` applied to both results independently
in Python: **identical deterministic structure** (the spec §15 preferred/expected outcome — not
just "close enough").

### Review-comparable benchmark profile, once, candidate-bound (spec §22 item 13, §23)

See [`v0.4.1-read-cost-benchmark.md`](v0.4.1-read-cost-benchmark.md) for the full candidate-bound
result and human-readable summary.

## Public documentation and changelog (spec §26)

`README.md`, `ROADMAP.md`, `docs/mcp.md`, `docs/specifications/0.4.1/README.md`, and
`docs/specifications/0.4.1/specification.md` were already brought to their bounded, truthful
pre-freeze form during I3.2 (PR #120, including its review round) — each correctly distinguishes
capability-completion (I1/I2 GO, true) from release-publication (not yet true — I3 marked "in
progress"/"in release qualification", never "shipped"). No further change required at this step.
`CHANGELOG.md`'s dated `[0.4.1]` entry and `docs/release-validation/v0.4.1-release-notes.md`'s draft
were likewise already added during I3.2.

## Candidate status

### Frozen: `bc03602f95557eb0450506a9f5c3a7f7c296b2da`

```text
RELEASE_CANDIDATE_SHA = bc03602f95557eb0450506a9f5c3a7f7c296b2da
```

This is the literal source commit containing the exact code, contracts, package identity (`0.4.1`),
and public release content intended for `v0.4.1`. No `v0.4.1-rc.1` tag is cut here — that is I3.3's
deliverable, against this exact candidate SHA.

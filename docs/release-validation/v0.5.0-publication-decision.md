# v0.5.0 Publication Decision

This is the repository owner's publication disposition required by I6
[§15](../specifications/0.5.0/i6-release-candidate-publication-and-post-release-verification.md)
(Draft 0.3). It is recorded separately from the technical evidence. The merge commit of the PR that
adds this record is `DECISION_COMMIT_SHA`. Per §4, the record does not cite its own commit.

## Disposition

```text
publication disposition = GRANTED
RELEASE_CANDIDATE_SHA   = 8f63c2369084df9c57535f9112fe2dfa17154505   (candidate attempt rc.1)
EVIDENCE_COMMIT_SHA     = fa8a72bc542d0aa774b4f849f162033f672e6a3f   (v0.5.0-release-readiness.md, #265)
```

The repository owner, Michael Egner, gave this disposition on 2026-09-26 (the question was
answered at 2026-09-26T18:02Z), in response to an explicit question in the release session. The
owner's merge of the PR carrying this record is the recorded authorization. No agent or automation
inferred it from the technical gates being green (§15).

## Scope of the authorization

`GRANTED` authorizes publishing **exactly** the qualified candidate above as `v0.5.0`, relying on
the evidence in [`v0.5.0-release-readiness.md`](v0.5.0-release-readiness.md). The owner's
confirmations recorded there include:
- the candidate-image Trivy disposition;
- the §10.6 link findings.

The authorization does not cover:
- any other commit, including later commits on `main`;
- a re-created or moved tag or release, which requires a new decision and a spec amendment
  (§17.2);
- any claim that `v0.5.0` shipped. Only `SHIPPED_VERIFIED` (§23, §28) permits that claim.

## Next (§17)

Immediately before publication, these are re-checked:
- `RELEASE_CANDIDATE_SHA` is unchanged;
- release readiness still applies;
- no later commit is substituted;
- the `v0.5.0` tag and GitHub Release are both absent. They were absent when this record was
  written.

The tag is then created at `RELEASE_CANDIDATE_SHA`, the GitHub Release is published, and the
release workflow runs. Slice 5 verifies the published artifacts.

# `v0.4.2-rc.1` Candidate Preparation — I3.2

Spec: [`docs/specifications/0.4.2/i3-client-qualification-and-release-preparation.md`](../specifications/0.4.2/i3-client-qualification-and-release-preparation.md)
§3 (Entry Conditions), §4 (Release Identity and Lifecycle), §8 (Candidate Qualification Gates —
the automated subset that does not depend on actual-client qualification).

This record is an evidence-only addition per spec §4.4 ("qualification reports", "retrospective
completion records" are named as allowed post-freeze changes) — it changes no executable behavior
and creates no new candidate. `RELEASE_CANDIDATE_SHA` is the exact `main` tip this record was written
against, unmodified by this record's own commit.

## Entry audit (spec §3)

| Condition | Result | Evidence |
|---|---|---|
| I1 candidate-bound completion evidence present | ✓ | `docs/specifications/0.4.2/i1-completion-record.md`, rebound by this record's own commit from its original retrospective `279c0ae` citation to `RELEASE_CANDIDATE_SHA` itself (a citation update — I1's suite is unmodified between those two SHAs) |
| I1 blockers = 0 | ✓ | Same record's exit statement |
| I2 completion record exists and reports GO | ✓ | `docs/specifications/0.4.2/i2-completion-record.md`'s exit statement |
| I2 blockers = 0 | ✓ | Same record |
| `/mcp` sole public MCP path | PASS | `docs/mcp.md`; `tests/unit/test_mcp_discovery.py` |
| Exactly three tools | PASS | `tests/unit/test_mcp_discovery.py`; live `tools/list` below |
| `schema_version = "0.4"` | PASS | `app/architecture_intelligence/contracts.py`'s `Literal["0.4"]`; every live response below |
| Direct/negotiated semantic equivalence | PASS | `tests/integration/test_mcp_negotiated_transport.py` (part of the clean-checkout run below) |
| I2 `--serve` deterministic fixture | PASS | `tests/integration/test_mcp_demo_script.py::TestServeLifecycle` (part of the clean-checkout run below); live rerun below |
| I2 fixture classifier | PASS | `tests/integration/test_check_fixture_state_classification.py` |
| I2 repeated-serve idempotency | PASS | `TestServeLifecycle.test_full_lifecycle`'s repeated-`--serve` step |
| Shell lint | PASS | `shellcheck examples/runtime-demo/*.sh` (I3.1's new CI gate) — clean |
| Conditional session | `NOT_APPLICABLE` | `i1-completion-record.md`: no session IDs issued in either mode |

No entry condition failed.

## Candidate identity and freeze (spec §4.3)

Version identity was already fully consistent at the current `main` tip before this record was
written — `app/version.py`'s `package_version()` is the single source of truth (reads
`pyproject.toml`), and every call site (`app/mcp/server.py`, `app/mcp/wiring.py`,
`evaluation/architecture_answers/runner.py`) imports it rather than hardcoding a literal (fixed
during `v0.4.1` I3.2 after a real version-drift bug — see `docs/specifications/0.4.1/i1-completion-record.md`'s
sibling record for that history). No fix commit was needed before freeze:

```text
RELEASE_CANDIDATE_SHA = 71e2d8b954fa92430723fced302baa3666255397
dirty_worktree = false
```

This is the `main` tip immediately after I3.1 merged (PR #141) — no commit landed between I3.1's
merge and this record.

### CI identity (spec §8.5)

Verified via the GitHub API against this exact commit (never `gh pr checks` or a PR's ambient
current-head view):

```bash
gh api repos/michaelegner/architecture-intelligence-platform/commits/71e2d8b954fa92430723fced302baa3666255397/check-runs
```

| Check | Status | Conclusion |
|---|---|---|
| `lint + test` | completed | success |
| `analyze (actions)` | completed | success |
| `analyze (python)` | completed | success |
| `dependency security scan (pip-audit, spec §29)` | completed | success |

All required checks `completed`/`success` at the exact candidate SHA.

## Version and package metadata (spec §8.4)

```text
pyproject.toml                                     version = "0.4.2"
uv.lock (root project entry)                        version = "0.4.2"
app.version.package_version()                       = "0.4.2"
app.mcp.server.mcp_server.version                   = "0.4.2"   (via package_version())
app.mcp.wiring's production-wired Producer.version  = "0.4.2"   (via package_version())
evaluation.architecture_answers.runner's Producer   = "0.4.2"   (via package_version())
```

`schema_version` remains `"0.4"` throughout (`Literal["0.4"]` in
`app/architecture_intelligence/contracts.py`; confirmed live in every response below).

`producer.build_revision`, confirmed against a real running server (not inferred from the checkout,
image tag, or workflow metadata):

```bash
export RELEASE_CANDIDATE_SHA="71e2d8b954fa92430723fced302baa3666255397"
BUILD_REVISION="${RELEASE_CANDIDATE_SHA}" examples/runtime-demo/mcp-demo.sh --serve
```

```json
"producer": {
  "name": "architecture-intelligence-platform",
  "version": "0.4.2",
  "build_revision": "71e2d8b954fa92430723fced302baa3666255397"
}
```

Exact match. This is also the first live proof that I3.1's `BUILD_REVISION` → `AIP_BUILD_REVISION`
bridge works end to end in a genuine release-gating invocation shape (`RELEASE_CANDIDATE_SHA`
exported, `BUILD_REVISION` set from it), not just in I3.1's own subprocess-driven integration tests.

The missing/mismatch rejection paths were likewise re-run live at this exact candidate, not only
inferred from the successful matching run above:

```bash
# missing BUILD_REVISION
RELEASE_CANDIDATE_SHA="71e2d8b954fa92430723fced302baa3666255397" \
  examples/runtime-demo/mcp-demo.sh --serve
```
```text
error: RELEASE_CANDIDATE_SHA is set but BUILD_REVISION is missing - release-gating runs require:
BUILD_REVISION="${RELEASE_CANDIDATE_SHA}" examples/runtime-demo/mcp-demo.sh --serve
exit code: 1
```

```bash
# mismatched BUILD_REVISION
RELEASE_CANDIDATE_SHA="71e2d8b954fa92430723fced302baa3666255397" \
  BUILD_REVISION="0000000000000000000000000000000000000000" \
  examples/runtime-demo/mcp-demo.sh --serve
```
```text
error: BUILD_REVISION (0000000000000000000000000000000000000000) differs from
RELEASE_CANDIDATE_SHA (71e2d8b954fa92430723fced302baa3666255397)
exit code: 1
```

Both fail during `validate_prerequisites`, before any `docker compose` call — confirmed via
`docker compose -f docker-compose.demo.yml ps` showing no containers created by either invocation.

## Clean-checkout qualification (spec §8.1-§8.2)

Run from a freshly created `git worktree --detach` at the exact candidate SHA
(`71e2d8b954fa92430723fced302baa3666255397`, `git status --porcelain` empty before starting) — never
the developer's mutable working tree.

```bash
uv sync --locked                        -> Resolved 65 packages, no lock drift
uv run ruff check .                     -> All checks passed
uv run ruff format --check .            -> 258 files already formatted
shellcheck examples/runtime-demo/*.sh   -> clean
uv run pytest tests/unit -q             -> 998 passed
uv run pytest tests/integration -q      -> 287 passed
uv run --with pip-audit pip-audit       -> No known vulnerabilities found
```

### Relation-fact evaluator (spec §8.2)

```bash
uv run python -m evaluation run
```

```text
Scenarios: 10, Passed: 10, Failed: 0
Missing facts: 0, Unexpected facts: 0, Forbidden facts present: 0
Wrong statuses: 0, Evidence errors: 0
RESULT: PASS
```

### Candidate-bound Architecture Answers evaluation and repeatability (spec §8.2-§8.3)

```bash
uv run python -m evaluation answers --candidate-sha 71e2d8b954fa92430723fced302baa3666255397
```

```text
result:                     PASS
scenarios / passed / failed: 23 / 23 / 0
run_count:                  2
semantic_outputs_identical: true
run_output_sha256[0] == run_output_sha256[1] (both sha256:511bc961...461712)
real_system_qualification:  quarkus-super-heroes PASS, apache-airflow PASS
cross_tool_invariants:      dependency_to_drift PASS, drift_to_evidence PASS
```

23/23 PASS, byte-identical output hash across both runs — repeatability confirmed, not merely
asserted.

### Deterministic hero path, twice from clean state (spec §8's direct-hero-demo/`--serve` gates)

Run twice from separate clean `docker compose -f docker-compose.demo.yml down -v` states, from the
same clean worktree, with `BUILD_REVISION`/`RELEASE_CANDIDATE_SHA` pinned as above both times:

```text
tools/list                     -> get_architecture_drift, get_evidence, get_service_dependencies
get_architecture_drift         -> LegacyPricingService OBSERVED_ONLY (via GET /pricing/{sku})
                                   unused-q NOT_OBSERVED_IN_WINDOW
get_evidence (same snapshot)   -> missing_evidence_refs: [] (fully resolved)
snapshot_id                    -> aip:snapshot:v1:685a34157b6842b00d7130b8490df63060c3d80871c2ed6f56cd9206e62342d8
                                   (identical both runs)
```

Both runs' full `get_architecture_drift` `structuredContent` compared byte-for-byte via independent
Python comparison (not visual inspection): **identical**, including `snapshot_id` and
`producer.build_revision`.

### Tool discovery, direct/negotiated equivalence, zero-write (spec §8.3)

All covered by the clean-checkout unit/integration runs above:
`tests/unit/test_mcp_discovery.py`, `tests/integration/test_mcp_negotiated_transport.py`,
`tests/integration/test_mcp_i1_zero_write_completion_gate.py` (added in I3.1),
`tests/integration/test_mcp_service_dependencies_equivalence.py`,
`tests/integration/test_mcp_architecture_drift_equivalence.py`,
`tests/integration/test_mcp_evidence_equivalence.py`.

## Repository hygiene (spec §8.6)

All seven prohibited classes checked against the fresh candidate worktree (not the developer's own
working directory, which is known to carry unrelated, untracked local WSL cruft outside this checkout
entirely):

| Class | Check | Result |
|---|---|---|
| `*:Zone.Identifier` | `find . -type f -name '*:Zone.Identifier' -print` | no matches |
| `.DS_Store` / `Thumbs.db` | `git ls-files \| grep -iE '\.DS_Store\|Thumbs\.db'` | no matches |
| Raw unsanitized traces | `docs/release-validation/v0.4.2-client-traces/` does not exist yet — no actual-client capture has happened (that's I3.3-I3.4); nothing to sanitize or leak at this candidate | none present |
| Temporary packet captures/logs | `git ls-files \| grep -iE '\.(pcap\|pcapng\|har\|log)$'` | no matches |
| Personal MCP configuration | `git ls-files \| grep -iE '(^\|/)\.cursor/mcp\.json$\|(^\|/)\.vscode/mcp\.json$\|(^\|/)\.mcp\.json$'` | no matches (I2's own DoD already required this; reconfirmed here) |
| Credentials | `gitleaks detect --source=. --no-git -v` (Docker image, full-tree scan, not just diff-scoped) | 2 findings, both false positives: `NEO4J_AUTH: neo4j/REDACTED` in two pre-sanitized `docs/real-world-validation/cross-system/artifacts/*.yaml` capture files from prior real-system validation work — the literal file content is the word `REDACTED`, not a real credential |

No unresolved hygiene finding.

## Link validation (spec §4.5, candidate-contained)

Every relative link in the candidate-relevant surface — root `README.md`, `docs/mcp.md`,
`examples/mcp-clients/{README,codex,claude-code,cursor,vscode}.md`, and every
`docs/specifications/0.4.2/*.md` file — resolves to a real path in this exact checkout, with fenced
code blocks and inline-code spans excluded from the scan (several apparent "broken" links are
illustrative markdown-inside-markdown, e.g. §35 of the I2 spec showing what the *root README* should
contain, not a literal link from that spec file itself). The only non-resolving pattern found is the
pre-existing, intentionally GitHub-relative `../../discussions` link in `README.md`, already noted as
out of scope in PR #137's own test plan.

A broader, repository-wide scan (570 relative links, all `.md` files) additionally surfaced four
**pre-existing, non-candidate** issues, none touched by I1/I2/I3 and none release-blocking for
`v0.4.2`: a stale `LICENSE` relative path in `docs/specifications/h5-open-source-readiness.md`, a
`v0.1.0` vs `0.1.0` directory-naming mismatch in `docs/specifications/0.2.0/README.md`, an
internal-style relative link that should be an external GitHub reference in
`docs/real-world-validation/quarkus-super-heroes/evidence/messaging.md`, and the two GitHub-relative
`../../discussions`/`../../issues/new/choose` links in `SUPPORT.md`. Recorded here for completeness;
not part of this candidate's scope to fix.

## `docs/mcp.md` matches the release candidate (spec §9.5)

Unchanged since I2.3 and independently corroborated by this record's own live checks above:
`/mcp` sole public path, direct + negotiated modes, stateless-by-default, exactly three tools, the
non-POST 405 HTTP method contract, `NOT_APPLICABLE` conditional-session semantics, and the local
security boundary added in I2.3 — no drift found.

## Deferred to later I3 sub-slices (not this record's scope)

- Actual-client qualification for Codex CLI, Claude Code, Cursor, VS Code (I3.3-I3.4).
- Cross-client semantic comparison and the mandatory separate UX observation (I3.5).
- The qualified client/platform matrix and README/I2-guide matrix links (I3.5).
- Draft release notes, the GO/NO-GO record, and the I3 completion record (I3.6).
- Publication and post-release verification (I3.7 — requires separate owner authorization).

## Candidate status

### Frozen: `71e2d8b954fa92430723fced302baa3666255397`

```text
RELEASE_CANDIDATE_SHA = 71e2d8b954fa92430723fced302baa3666255397
```

This is the literal source commit containing the exact code, contracts, and package identity
(`0.4.2`) qualified by this record's full automated gate table. No `v0.4.2-rc.1` tag is cut here —
tagging requires separate owner authorization and follows actual-client qualification (I3.3-I3.7).

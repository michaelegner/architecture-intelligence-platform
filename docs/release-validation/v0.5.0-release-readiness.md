# v0.5.0 Release Readiness — `rc.1`

This is the exact-candidate pre-publication evidence required by I6
[§13](../specifications/0.5.0/i6-release-candidate-publication-and-post-release-verification.md)
(Draft 0.3), for Slice 2. The merge commit of the PR that adds this record is
`EVIDENCE_COMMIT_SHA`. Per §4, this record does not cite its own commit.

```text
technical state = RELEASE_READY
```

`RELEASE_READY` is not publication authorization. Publication needs a separate decision by the
repository owner (§15, Slice 3).

## Identities

| Identity | Value |
|---|---|
| `I5_QUALIFIED_SEMANTIC_SHA` | `aa04a150965924bd23ecf0125bcf7095a3cf72d9` |
| `I5_COMPLETION_SHA` | `1ebca5e96a24156179f0a57b5abbdfe9b51c1ee7` |
| `RELEASE_CANDIDATE_SHA` | `8f63c2369084df9c57535f9112fe2dfa17154505` (candidate attempt `rc.1`; the #264 merge, 2026-09-26T17:19:36Z) |
| Earlier candidate attempts | none |

## Clean-checkout proof

Qualification ran in one fresh, isolated clone from GitHub, at one fixed absolute path in the
qualifier's session workspace. The clone was checked out detached at the candidate:

```text
test "$(git rev-parse HEAD)" = 8f63c2369084df9c57535f9112fe2dfa17154505   -> pass
test -z "$(git status --porcelain)"                                        -> pass
```

The checkout was still clean after every gate: the unit and integration suites, both evaluation
runs, the image build and the golden path. Every command received the candidate SHA explicitly,
never from `HEAD`.

## Candidate identity (§11, §13)

| Field | Value |
|---|---|
| Package version: `pyproject.toml`, the `uv.lock` root, `package_version()`, the MCP server | `0.5.0` in all four (`v0.5.0-artifacts/rc.1/version/surfaces.json`) |
| `schema_version` | `"0.5"` |
| `uv.lock` SHA-256 | `b786fbfbbe5eb99d29bda6cc0033284d908ad8604f9f7ef639c53fccb71e8a9e` |
| `schemas/architecture_intelligence/v0.5` git tree | `02db433c773ebd17df17b016b647218025d65ad0`, identical to the I5 record |
| `schemas/import/v0.5` git tree | `953b15e590ffbb1caa5bda89539e5f164f618466`, identical to the I5 record |
| Snapshot canonicalization | `_CANONICALIZATION_VERSION = 3` |
| Import report | `aip-import-report/1` |
| Adapters | `openapi-adapter@1` v1, `asyncapi-adapter@1` v1, `manifest-adapter@1` v1, `kubernetes-adapter@1` rule 1 |
| Deployment reconciliation | `service-workload-reconciliation`, rule version 1 |
| Candidate image | `aip-v0.5.0-candidate:<sha>`, id `sha256:f21ef41202066f0b719971b9565f5bcd009ad136c298b16a66951229f26947f1` (`docker build --no-cache --build-arg AIP_BUILD_REVISION=<sha>`; a local build only) |
| Resolved base images | `python:3.14-slim@sha256:51dafde81dbdb6ebde285137a295cf18a47ca95234fe388a343719cb97305b3d` and `ghcr.io/astral-sh/uv:latest@sha256:04d046b13e60d6bcec73cbc5e1cad25d680dea90c8573340950a0ac2d1aef424`, from BuildKit's resolve lines in the build log |
| Image runtime user / `AIP_BUILD_REVISION` | `app` / `8f63c2369084df9c57535f9112fe2dfa17154505` |
| Live `producer` (golden path) | `{"name": "architecture-intelligence-platform", "version": "0.5.0", "build_revision": "8f63c2369084df9c57535f9112fe2dfa17154505"}` |

### Fixture identities

| Fixture | Identity |
|---|---|
| I2 independent capture | `tree_digest` `ae995c5f25649861c381c9870aff98cd6dbc3f9e137940fada2016227d156bb6` |
| I3 cross-source | `e4034677a15053215fa7a89d744fde04e56a9206e46ca3bf48983e158096e9bd` |
| I4 Pub/Sub (ASB and GCP) / Kafka | `1dd57b82092c43f452328fcb474bcc8d978db127507391370e8dc7934cf06ca3` / `326c4bf3e0c90561041d1f306ea00533470041a19bf664142d61da8b92acf67a` |
| Evaluation scenarios | `a6b1ded5debf5daf2a8e2fd173eaf993e8acc97baccc168a3da5dc1a0dda13ca` at the candidate. With `producer.version` reverted in a copy, this is `98ab04c8cf0a464ab88dfdc423ea261616ee1f7493f5261ed24615872c039aa0`, the I5 pin, asserted by `test_i5_lifecycle_freeze.py`. |
| Golden-path profile | `profile/` tree `91af9faeb8b8876cf2a09a038548aa7091eb9ff7`; `expected.json` SHA-256 `8b79e6aacb07510fee274ca81ea9501aca8e024b5e02f0de370b4b12cacd14da`; `SHA256SUMS` SHA-256 `baad428c45d64db4e302b461fe6a01dd9222ba78ac58c24531e5c8ce70b49457` |

The first four digests are the `docs/real-world-validation/v0.5.0/coverage-matrix.md` pins, which
`tests/unit/test_i5_lifecycle_freeze.py` asserts at the candidate.

### Tools

| Tool | Identity |
|---|---|
| Trivy | `aquasec/trivy:0.70.0@sha256:be1190afcb28352bfddc4ddeb71470835d16462af68d310f9f4bca710961a41e`. This is the same Trivy version the release workflow pins (`version: v0.70.0`). |
| gitleaks | `zricethezav/gitleaks@sha256:c00b6bd0aeb3071cbcb79009cb16a60dd9e0a7c60e2be9ab65d25e6bc8abbb7f` (`v8.30.1`) |
| pip-audit | `uv run --with pip-audit pip-audit`, the same command CI runs |
| Neo4j and collector (golden path) | Digest-pinned in `examples/release-golden-path/compose/docker-compose.base.yml` |

## Gate results

### CI at the exact SHA

Read from the GitHub check-runs API for `8f63c23…`, not `gh pr checks`. The table is in
`v0.5.0-artifacts/rc.1/ci/ci-check-runs.tsv`. There were no rerun attempts.

| Check | Conclusion |
|---|---|
| `lint + test`, `quality`, `integration-core`, `demo-e2e` | success |
| `dependency security scan (pip-audit, spec §29)` | success |
| CodeQL `analyze (python)`, `analyze (actions)` | success |
| `update-uv-graph`, Dependabot (3) | success |

### §10.1 source qualification

These ran in the clean checkout. Summaries are in `v0.5.0-artifacts/rc.1/gates/`.

| Gate | Exit | Result |
|---|---|---|
| `uv sync --locked` | 0 | lock satisfied |
| `ruff check .` | 0 | All checks passed |
| `ruff format --check .` | 0 | 374 files already formatted |
| `pytest tests/unit` | 0 | **2273 passed** |
| `pytest tests/integration` | 0 | **466 passed** |
| `uv run --with pip-audit pip-audit` | 0 | No known vulnerabilities found |

### §10.2 version consistency

`tests/unit/test_release_version_consistency.py` passed (7 tests). The package, lock root,
`package_version()`, MCP server and live `producer.version` are all `0.5.0`. `schema_version` is
`"0.5"`.

### §10.3 Architecture Answer evaluation, run twice

Both runs used `python -m evaluation answers --candidate-sha 8f63c23…` from the same absolute
checkout. Each run's report was copied aside, and the checkout was restored after each run.

| | Run 1 | Run 2 |
|---|---|---|
| Result | PASS, 23/23 | PASS, 23/23 |
| `semantic_outputs_identical` | true | true |
| `candidate_sha` | `8f63c23…` | `8f63c23…` |
| SHA-256 | `5a030706408d9d3b5be6af95f6588f632831c16b8239d769377a73a987160e85` | the same |

`cmp` shows the two runs are byte-identical: `repeatability = PASS`. The committed
`evaluation/architecture_answers/results/architecture-answers-evaluation-result.json` is refreshed
in this evidence PR to run 1, which identifies `RELEASE_CANDIDATE_SHA`. It is evidence-only
(§10.3). The `v0.5.0` tag carries the pre-refresh report (`a906a58…`), as the release notes state.

### §10.4 I1-I4 capability gates

Each gate is demonstrated by named tests that passed inside the full suites above.

| Gate | Tests |
|---|---|
| Source-adapter conformance; OpenAPI/AsyncAPI supported corpus | `tests/unit/test_openapi_adapter.py`, `test_asyncapi_adapter.py`, `test_manifest_adapter.py`, `test_source_validation.py`, `test_sources_service_identity.py` |
| Inventory, scope, tombstone and discovery-failure safety | `tests/unit/test_sources_inventory.py`, `test_sources_tombstones.py`, `tests/integration/test_import_report.py`, `tests/unit/test_i5_lifecycle_freeze.py` |
| Reimport, conflict and atomicity | `tests/unit/test_sources_claim_conflicts.py`, `test_sources_claim_reconciliation.py`, `tests/integration/test_importer.py` |
| Kubernetes offline discovery | `tests/unit/test_ingestion_kubernetes_*.py`, `test_sources_kubernetes_*.py`, `tests/integration/test_kubernetes_independent_capture.py`, `test_kubernetes_surface_regression.py` |
| Runtime Service/Workload reconciliation | `test_architecture_intelligence_deployment_{reconciliation,projection,repository,path_c,evidence_visibility}.py`, `tests/integration/test_i3_cross_source_qualification.py` |
| Pub/Sub, positive and negative | `tests/integration/test_i4_pubsub_qualification.py` (ASB, GCP, Kafka), `test_pubsub_*.py`, `test_asyncapi_adapter_pubsub.py`, `test_messaging_guards_pubsub.py` |
| Public schema freeze | `test_architecture_intelligence_schema_frozen.py` |
| Exactly three MCP tools | `test_mcp_discovery.py`, `test_mcp_negotiated_transport.py` |
| Read-only invariants | `test_mcp_read_only_boundary.py`, `test_mcp_i1_zero_write_completion_gate.py` |
| REST / service / negotiated-MCP parity | `test_api_architecture_intelligence_equivalence.py`, `test_mcp_{service_dependencies,architecture_drift,evidence}_equivalence.py`, `test_mcp_drift_scenario_parity.py` |

### §10.5 I5 inheritance gate

`git diff aa04a15 8f63c23` touches **no file** under `app/`, `schemas/`, `Dockerfile`, `config.yaml`,
`config.demo.yaml` or `config/`. The changed paths are:
- the I5 records (`docs/real-world-validation/`, `docs/specifications/`);
- `examples/release-golden-path/` (the I6 profile and harness);
- `examples/mcp-clients/`, `README.md` and `docs/*.md` (documentation);
- `.github/workflows/` (the I6 §8 release workflow and the shellcheck glob);
- the `0.5.0` version surfaces (`pyproject.toml`, `uv.lock`);
- tests;
- the 23 evaluation scenario fixtures, `producer.version` only. The version-normalized digest above
  equals the I5 pin.

The release preparation introduces no architecture-semantic change relative to the I5 qualified
baseline, so the full Quarkus and Airflow reruns are not required.

### §11 candidate image and the golden path

`RELEASE_CANDIDATE_SHA=8f63c23… examples/release-golden-path/run.sh
aip-v0.5.0-candidate:8f63c23… <out>`, run from the clean checkout, exits 0. The results are in
`v0.5.0-artifacts/rc.1/golden-path/`.

| Phase | Checks passed | Covers |
|---|---|---|
| `demo` | 12 / 12 | OpenAPI, AsyncAPI Queue, deterministic OTel, fixture state `COMPLETE` (snapshot `0bfcbded…`), REST and negotiated MCP, reconnect, cross-surface equality, zero writes |
| `pubsub` | 8 / 8 | Topic, explicit Subscriptions, observed Pub/Sub, the full graph-fact multiset, REST and MCP |
| `k8s-agree` | 6 / 6 | Kubernetes offline discovery; `RESOLVED_EXPLICIT` supported by Paths A, B and C; evidence drill-down |
| `k8s-conflict` | 5 / 5 | `CONFLICT` on both Services, with no guessed association |
| `k8s-unresolved` | 5 / 5 | two `UNRESOLVED` resolutions through the public projection |

Every phase passed `run-identity`: the running image equals `IMAGE_REF`, `AIP_BUILD_REVISION`
equals the candidate SHA, and the user is `app`. Every phase also passed `producer-identity`.

### §12 candidate security

| Category | Result |
|---|---|
| Candidate source | pip-audit: no known vulnerabilities. CodeQL `analyze (python)` and `analyze (actions)`: success at the SHA. |
| Candidate image | Trivy 0.70.0, `HIGH,CRITICAL`, `--ignore-unfixed=false`: **46 HIGH, 0 CRITICAL**. Full JSON in `v0.5.0-artifacts/rc.1/security/trivy-candidate.json`; disposition below. |
| Final published image | Not applicable before publication (§19). |
| Secrets | gitleaks over a `git archive` export of the candidate's tracked files: 38 hits, all false positives (below). No record in this PR contains a secret; the golden-path Neo4j password is generated per run and never written. |
| Non-root | `whoami` returns `app` (image and running container) |
| Release workflow least privilege | `docker.yml` permissions unchanged (`contents: read`, `packages: write`, `security-events: write`), pinned by `tests/unit/test_docker_release_workflow.py` |

**Candidate-image finding disposition.** The repository owner decided this disposition before this
record; merging this PR confirms it.

| Origin | Findings | Fix available | Disposition |
|---|---|---|---|
| Debian base-image OS packages (`util-linux` family, `ncurses`, `libsystemd0`/`libudev1`, `libacl1`, `perl-base`) | 44 HIGH | none: 43 `affected`, 1 `fix_deferred` | `KNOWN_LIMITATION`, reviewed and not release-blocking. They are inherited from `python:3.14-slim`, and the v0.4.2 base-image precedent applies. |
| `msgpack` 1.1.2 (GHSA-6v7p-g79w-8964, fixed in 1.2.1) and `setuptools` 70.3.0 (CVE-2025-47273, fixed in 78.1.1) | 2 HIGH | yes, upstream | `KNOWN_LIMITATION`, reviewed and not release-blocking. Both are vendored inside the base image's own system `pip` 26.2.1 (`/usr/local/lib/python3.14/site-packages/pip/_vendor/vendor.txt`). Neither is in the AIP application environment, and AIP never runs `pip` at runtime. |

No finding is in an AIP dependency. The final published image gets its own disposition, bound to
its digest, at §19.

**gitleaks disposition (38 hits, all false positives).**
- **5 × `NEO4J_AUTH: REDACTED`:** already-sanitized placeholders in the I5 and v0.4.2 records.
- **32 in the Quarkus Super Heroes manifests** (`kubernetes-secret-yaml` and `generic-api-key`).
  They sit in the byte-identical upstream `deploy/k8s/java25-kubernetes.yml` at pin `8ea0337` and
  its namespace-only derived copy, both frozen I5 inputs. The values decode to the public upstream
  demo credentials (`locations`, `superbad`, `superfight`, …), which also appear in the upstream
  Compose files.
- **1 × a SHA-256 constant** (`tests/unit/test_airflow_v05_dossier.py:29`).

### §10.6 repository and documentation hygiene

- **Content gate re-applied** to the 1114 files added or changed in `v0.4.2..8f63c23`:
  - no `.env`, keys or certificates, and no Neo4j data or IDE metadata;
  - no secret-key patterns in the added lines;
  - no internal host names.
- **Two disclosed items, both non-secret:**
  - The I5 records deliberately carry run logs (`*.log`, force-added after a secret scan) as
    qualification evidence.
  - The I5 run scripts and records reference the qualifier's local checkout path and session
    workspace paths. I5 froze its one absolute checkout location by design (I5 §12). Password
    *files* are referenced by path only; no password value is recorded.
- **Links:** 715 relative links in `README.md`, `CHANGELOG.md` and 204 files under `docs/**/*.md`
  were checked against the git tree at the SHA (`v0.5.0-artifacts/rc.1/hygiene/links.txt`). Four
  do not resolve, and all four already existed at `v0.4.2`. They are a §10.6 finding, dispositioned
  as `KNOWN_LIMITATION` by the owner's decision: documentation only, and none affects identity,
  the contract, security or the golden path (§14).
  - `README.md` → `../../discussions`: a GitHub route that resolves on GitHub. This is an
    intentional exception.
  - `docs/real-world-validation/quarkus-super-heroes/evidence/messaging.md` → `../event-statistics`:
    verbatim upstream text. This is an intentional exception.
  - `docs/specifications/0.2.0/README.md` → `../v0.1.0/README.md`: the directory is `0.1.0`. To fix
    on `main` after the release.
  - `docs/architecture-review-0.4.0.md` → `../app/ingestion/pipeline.py`: the file was removed in
    v0.5 I1 PR3a. To fix on `main` after the release.

### §10.7 Kubernetes mode

v0.5.0 releases Kubernetes discovery as **`OFFLINE_ONLY`**. Live-RBAC, denied-verb and denied-scope
qualification does not apply and is not claimed.

## §14 finding ledger

| Id | Finding | Classification |
|---|---|---|
| R1 | 44 Debian base-image OS HIGH findings, no fix available | `KNOWN_LIMITATION` |
| R2 | `msgpack` and `setuptools` vendored in the base image's system `pip` | `KNOWN_LIMITATION` |
| R3 | 4 non-resolving relative links in historical docs (2 intentional, 2 to fix after the release) | `KNOWN_LIMITATION` |
| R4 | The Dockerfile's base images are mutable tags (`python:3.14-slim`, `uv:latest`), so an image rebuilt later may differ. The resolved digests are recorded above. | `KNOWN_LIMITATION` |
| R5 | `docker.yml` (§8) can run only at publication, so its first real execution is §17 | `KNOWN_LIMITATION` (§25 limitation, disclosed) |

**Unresolved release blockers: 0.**

Carried from I5 (release notes, "Known limitations"): F3 `DOCUMENT_UNSUPPORTED`; F5 and F8 `DEFER`.
The v0.4.2 client matrix is not re-qualified for v0.5.0.

## Required technical result (§13)

```text
semantic mismatches           = 0
unexpected canonical facts    = 0
guessed identities            = 0
unresolved release blockers   = 0
repeatability                 = PASS
producer.build_revision       = RELEASE_CANDIDATE_SHA (8f63c2369084df9c57535f9112fe2dfa17154505)
```

The semantic figures are the evaluation's 23/23 with zero mismatches, the golden path's zero failed
checks, and the I5 final-candidate figures inherited through the §10.5 gate.

```text
technical state = RELEASE_READY
```

The next step is Slice 3: the repository owner's publication decision
(`v0.5.0-publication-decision.md`). It names both `RELEASE_CANDIDATE_SHA` and this record's
`EVIDENCE_COMMIT_SHA`.

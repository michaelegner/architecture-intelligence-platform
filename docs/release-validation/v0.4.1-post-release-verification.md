# `v0.4.1` Post-Release Verification

Spec: [`docs/specifications/0.4.1/i3-hardening-qualification-and-release.md`](../specifications/0.4.1/i3-hardening-qualification-and-release.md)
§32 (Post-Release Verification), §34's precedent no-substitution discipline ("RC image != final
image": this record independently re-verifies the actual published `v0.4.1` artifact rather than
inheriting `v0.4.1-rc.1`'s own qualification in `v0.4.1-go-no-go.md`).

## Release identity

```text
Release URL:                 https://github.com/michaelegner/architecture-intelligence-platform/releases/tag/v0.4.1
Tag SHA:                      bc03602f95557eb0450506a9f5c3a7f7c296b2da
GitHub Release published:     2026-09-10T20:45:14Z
GO-record revision:           9edb03e1ece9fcaf952eeef3227d0e10e3641eb1 (PR #122 merge — the evidence commit)
Decision commit:              9464c651caaa2491da5ec58f79527a592ca8d611 (PR #123 merge — GO recorded)
```

`git rev-list -n1 v0.4.1` resolves to `bc03602f95557eb0450506a9f5c3a7f7c296b2da` — the exact
candidate named in `v0.4.1-go-no-go.md`'s GO decision; the tag was cut directly at that commit, not
a newer `main` HEAD (which had already advanced to the evidence/decision/final-publication commits
by the time of tagging).

## Docker publication workflow

```text
Trigger:      release (v0.4.1)
Run ID:        34528243787
Job:           build, scan, and push
Conclusion:    success
Count:         exactly one run for this release event
```

## GHCR artifact

| Check | Result |
|---|---|
| `v0.4.1` tag pull | Succeeded, anonymous (`docker logout ghcr.io` run first, no `docker login`) |
| `latest` tag pull | Succeeded, anonymous |
| `v0.4.1` digest | `sha256:fdcd5957419b0c6c4e90536b529b08b88a49573e9bb9ea256fa090e2ed3c76fa` |
| `latest` digest | `sha256:fdcd5957419b0c6c4e90536b529b08b88a49573e9bb9ea256fa090e2ed3c76fa` |
| Digest equality | Equal |
| Non-root execution | `docker exec ... whoami` → `app` |
| `GET /health` | `{"status":"ok"}` |
| `GET /health/neo4j` | `{"status":"ok"}` |
| `POST /api/import` | Succeeded — all 4 bundled `examples/` services imported |

Note: the final image digest (`sha256:fdcd5957...`) legitimately differs from the `v0.4.1-rc.1` RC
digest (`sha256:69b4eb38...`) — expected, since the release workflow rebuilds on publication rather
than re-publishing the RC's own image bytes (moving base-image references). Source SHA and workflow
provenance above are the binding identity; every check below was run fresh against this final
artifact, on a fresh disposable Neo4j and isolated Docker network, not inherited from the RC's own
qualification.

## Final MCP golden path (mandatory even though the RC passed)

Full golden path — both independent flows — executed against the pulled `v0.4.1` image itself,
fresh Neo4j, fresh import (no evidence seeded — a purely declarative import already exercises both
tools' non-`CONFIRMED` qualification paths):

```text
1. docker pull (anonymous)                                         VERIFIED
2. GET /health                                                       VERIFIED  {"status":"ok"}
3. GET /health/neo4j                                                 VERIFIED  {"status":"ok"}
4. POST /api/import                                                  VERIFIED  4 services imported
5. tools/list                                                        VERIFIED  exactly 3, correct
                                                                                order

Dependency -> evidence flow:
6. get_service_dependencies(service:order-service)                  VERIFIED  outcome=PARTIAL,
                                                                                unused-q/
                                                                                PaymentService/
                                                                                ProductService all
                                                                                NOT_OBSERVED_IN_WINDOW
7. get_evidence (same snapshot_id)                                   VERIFIED  4 records resolved,
                                                                                0 missing

Drift -> evidence flow:
8. get_architecture_drift(service:order-service)                    VERIFIED  identical claims to
                                                                                step 6 (correct -
                                                                                nothing is CONFIRMED
                                                                                yet, so every
                                                                                candidate is drift)
9. get_evidence (same snapshot_id)                                   VERIFIED  4 records resolved,
                                                                                0 missing

10. producer.version / producer.build_revision                      VERIFIED  0.4.1 /
                                                                                bc03602f95557eb04...
                                                                                (both flows, exact
                                                                                match to candidate)
```

## Tagged-source verification

```text
Method:                genuine `git clone --branch v0.4.1 --depth 1` from a clean scratch
                        directory (not a worktree of the existing checkout)
Checked-out commit:     bc03602f95557eb0450506a9f5c3a7f7c296b2da  (== GO candidate)
Package version:        0.4.1
uv sync --locked:       succeeded
Required files present: three v0.4 schemas, docs/mcp.md, examples/runtime-demo/hero-demo.md,
                        benchmarks/README.md
Light test subset:      tests/unit/test_release_version_consistency.py +
                        tests/unit/test_mcp_discovery.py — 6 passed
```

Lighter than I3.2's own clean-checkout qualification by design — the candidate is unchanged and was
already fully qualified end to end in I3.2/I3.3; this step confirms the *tagged, published* source
is genuinely that same content, not a full re-qualification.

## Trivy review

Queried scoped to the exact final tag ref (`?ref=refs/tags/v0.4.1`), same discipline the `v0.4.0`
post-release record established after finding an earlier unscoped-query gap:

```text
CVE-2026-59890        medium   setuptools MANIFEST.in exclusion bypass
CVE-2025-47273         high    setuptools path traversal
GHSA-6v7p-g79w-8964    high    msgpack out-of-bounds read on Unpacker reuse after caught error
```

Same three findings already open at `v0.4.1-rc.1`'s tag (confirmed in `v0.4.1-go-no-go.md`) and at
`v0.4.0`'s own tag — not new. Independently re-verified inside the actual pulled `v0.4.1` GHCR image
(not inferred from the alert text): `python -c 'import setuptools'` / `import msgpack` both raise
`ModuleNotFoundError` in the app's venv Python — neither package is installed or importable
application code, the identical disposition `v0.4.0`'s own post-release record established (a
base-image/`pip`-vendoring characteristic, not something this project's dependency pins control).

```text
Known critical release-blocking security findings = 0
Unreviewed release-blocking image findings        = 0
```

## Known post-release issues

**The published release body's post-candidate documentation links are stale, and require the same
correction `v0.4.0`'s own "Release-note evidence-link closure" step applied.** The live `v0.4.1`
release body's `README.md`/`ROADMAP.md`/`CHANGELOG.md`/`v0.4.1-go-no-go.md` links are pinned to
`9464c65` (the GO-decision merge, per the plan recorded in `v0.4.1-release-notes.md`) — at that
revision, `README.md` still names `v0.4.0` as latest, `ROADMAP.md` still labels `v0.4.1` "in
release qualification," and the GO record still says final publication/I3.4 are pending. The URLs
return `200`, but the content they lead to contradicts this record's own shipped/closed status
(review finding on PR #125).

This is a distinct correction from the earlier draft-preamble-leak class of mistake `v0.4.0`'s own
"Release body correction" section documents — that specific issue did *not* recur here (checked
directly against `v0.4.0`'s actual published body via `gh release view v0.4.0 --json body` before
publishing, and the live `v0.4.1` body correctly omits the internal preparer preamble). This is the
separate, later "Release-note evidence-link closure" step `v0.4.0` also needed — re-pointing the
post-candidate documentation links to the actual closure commit, which cannot exist before this PR
merges (the same chicken-and-egg the GO decision itself had, resolved the same way: act in a
follow-up commit once the real SHA exists).

**Planned fix, to be executed and recorded here once this PR merges:** confirm by direct fetch that
`README.md`/`ROADMAP.md` at the merge commit actually show shipped-state content, then
`gh release edit v0.4.1 --notes-file <corrected>` (body only — the tag itself is never touched) to
re-point the four post-candidate documentation links to that merge commit, verifying all resulting
URLs return `200` before applying the edit. Candidate-bound artifact/source links
(`LICENSE`, `docs/mcp.md`, `benchmarks/README.md`, schemas, the Quick Start clone) stay pinned to
`bc03602` — only the surfaces that keep evolving after candidate freeze move.

## Result

All post-release checks pass. `v0.4.1` is a real, published release with one auditable identity
from source candidate through GHCR artifact: candidate SHA → RC tag (`v0.4.1-rc.1`) → GO decision →
`v0.4.1` tag → GitHub Release → Docker workflow → GHCR digest, every link independently verified,
not assumed.

```text
SHIPPED — v0.4.1 at bc03602f95557eb0450506a9f5c3a7f7c296b2da hardens the qualification and
messaging semantics v0.4.0 shipped and commits reproducible evidence of current whole-graph read
cost, adding no discovery source, Canonical Model family, or MCP tool. The published source and
GHCR image identify the same immutable source revision, expose exactly three read-only MCP tools
with producer.version=0.4.1 and producer.build_revision=bc03602f95557eb0450506a9f5c3a7f7c296b2da,
and complete the independent dependency/drift/evidence golden path. Qualification mismatches and
coverage mismatches both remain 0, both messaging guards remain reachable with zero semantic
artifacts from a refused span, and the committed read-cost benchmark reproduces the current
whole-graph read-cost shape without any snapshot-cache or retention change. Release blockers and
post-release blockers are both 0.
```

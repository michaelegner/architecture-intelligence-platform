# `v0.3.0` Release Readiness — GO / NO-GO

**Date:** 2026-09-02
**Repository:** `michaelegner/architecture-intelligence-platform`
**Prepared per:** [`i5-release-qualification.md`](../specifications/0.3.0/i5-release-qualification.md)
(I5.2 — Final Qualification and GO/NO-GO)

## Candidate

| | |
|---|---|
| Candidate commit | `eeff933da3a8f1e5fc6749cb9a77bbf2e5123fd1` |
| RC tag | `v0.3.0-rc.2` (dereferenced directly via `git rev-list -n1 v0.3.0-rc.2` — matches) |
| Package version | `0.3.0` (`pyproject.toml`, `uv.lock` consistent — one-line lock diff, no dependency re-resolution) |
| Dependency-lock SHA-256 | `484361e9e5fc5f498809549cc413e4545832cd91e8720e8bb16406dcdfca0644` |
| Qualification method | isolated `git worktree` at the exact candidate SHA for each qualification pass — never the ambient development checkout |
| Superseded candidate | `v0.3.0-rc.1` @ `9f95d48046ab1942bb1a77c9a3a887a542120b98` — remains immutable, unmoved; superseded because it still declared package version `0.2.0` (spec §10.1) |

## Source qualification (spec §16)

| Check | Result | Evidence |
|---|---|---|
| `uv sync --locked` | PASS | Resolved/installed cleanly on the isolated worktree |
| `ruff check .` | PASS | "All checks passed!" |
| `ruff format --check .` | PASS | 159 files already formatted |
| Unit tests | PASS | 508 passed |
| Integration tests | PASS | 160 passed (Testcontainers Neo4j) |
| `pip-audit` | PASS | No known vulnerabilities found |
| Evaluation suite | PASS | `10/10`, twice — see below |
| Repeated-run determinism | PASS | Two runs, **raw stdout byte-identical** (SHA-256 `3ec3d35cb6ffa8ded38a936a5feacfc3e21ffea82c7fb17277d281113ca8d279` both times) — no normalization was needed at all, unlike the allowance spec §17 permits |
| Evaluation requires no LLM key | PASS | `OPENAI_API_KEY` unset throughout |

```text
Scenarios:          10
Passed:             10
Failed:             0

Missing facts:      0
Unexpected facts:   0
Forbidden facts present: 0
Wrong statuses:     0
Evidence errors:    0

RESULT: PASS
```

## Real-system evidence gate (spec §18)

The candidate changed from I4.4's `9f95d48` to `eeff933` (spec §18.2 applies — §18.1's
candidate-unchanged shortcut does not). Full fresh Quarkus + Airflow revalidation was executed, two
runs each, from clean state, bound to the literal new candidate. Full record:
[`v0.3.0-rc.2-real-system-revalidation.md`](v0.3.0-rc.2-real-system-revalidation.md).

| System | Correct | Unsupported | Unresolved | Insufficient | Incorrect supported |
|---|---:|---:|---:|---:|---:|
| Quarkus Super Heroes | 38 | 2 | 0 | 0 | 0 |
| Apache Airflow | 9 | 3 | 2 | 1 | 0 |

Matches the I5 spec §18.2 expected baseline exactly. Both systems' captures and comparator reports
are **byte-identical to I4.4's own frozen artifacts** — the version/lock fix touched no application
code, and this run proves that with fresh evidence rather than asserting it by content-equivalence
argument. No new finding; no ledger disposition changed.

## Public usage qualification (spec §19)

| Check | Result | Evidence |
|---|---|---|
| Quick Start — isolated worktree, `cp .env.example .env`, `docker compose up -d` (builds from source at the exact candidate) | PASS | Built and started cleanly |
| `GET /health` | PASS | `{"status":"ok"}` |
| `GET /health/neo4j` | PASS | `{"status":"ok"}` |
| `POST /api/import` | PASS | All 4 bundled `examples/` services imported, 0 errors |
| Quick Start teardown (`docker compose down -v`) | PASS | Clean removal |

Full record: [`v0.3.0-rc.2-real-system-revalidation.md`](v0.3.0-rc.2-real-system-revalidation.md)'s
WP4 section.

## RC GHCR artifact qualification (spec §21)

| Check | Result | Evidence |
|---|---|---|
| Release-triggered Docker workflow ran exactly once for `v0.3.0-rc.2` | PASS | [run 33626979845](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/33626979845), event `release`, `head_sha=eeff933`, conclusion `success` |
| Tagged image pull | PASS | `docker pull ghcr.io/michaelegner/architecture-intelligence-platform:v0.3.0-rc.2` succeeded |
| Digest recorded | `sha256:91c5665304ec9bacbbb7963c723ba9c0313b67ea41de5958a53dfae849975938` | `docker inspect --format='{{index .RepoDigests 0}}'` |
| Anonymous pull | PASS | No `docker login` performed for either pull in this qualification |
| Container runs as non-root | PASS | `docker exec ... id` → `uid=1000(app) gid=1000(app)` |
| `GET /health` (pulled image + Neo4j, not a local build) | PASS | `{"status":"ok"}` |
| `GET /health/neo4j` (pulled image) | PASS | `{"status":"ok"}` |
| `POST /api/import` (pulled image) | PASS | All 4 bundled services imported |
| `v0.3.0-rc.2` digest == `latest` digest | PASS | Both `sha256:91c56653...` |

## CI, CodeQL, and Trivy qualification (spec §20)

All checks queried directly against the exact candidate SHA (`eeff933`) via the GitHub Checks API.

| Check | Result | Evidence |
|---|---|---|
| CI — `lint + test` | PASS | [run 33622796241](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/33622796241) |
| CI — `dependency security scan (pip-audit)` | PASS | same run |
| CodeQL — `analyze (python)` | PASS | [run 33622796269](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/33622796269) |
| CodeQL — `analyze (actions)` | PASS | same run |
| Docker — `build, scan, and push` | PASS | [run 33626979845](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/33626979845), triggered once by the `v0.3.0-rc.2` release |

### Trivy review (spec §20's explicit review requirement — non-blocking exit does not mean auto-approved)

Three open code-scanning alerts on `refs/tags/v0.3.0-rc.2` at review time:

| Alert | Package | Trivy severity | Reviewed |
|---|---|---|---|
| `CVE-2026-59890` | `setuptools` (referenced version `70.3.0`) | MEDIUM | Not release-blocking |
| `CVE-2025-47273` | `setuptools` (referenced version `70.3.0`) | HIGH | Not release-blocking |
| `GHSA-6v7p-g79w-8964` | `msgpack` (referenced version `1.1.2`) | HIGH | Not release-blocking |

**Independently verified inside the actual pulled GHCR image** (not inferred from the alert text):
neither package is importable in the app's venv Python nor the base-image system Python —
`python -c 'import setuptools'` / `import msgpack` both raise `ModuleNotFoundError`. Both version
strings originate solely from pip's own bundled `_vendor/vendor.txt` and `_vendor/bom.cdx.json` —
an SBOM/vendoring-policy manifest pip ships describing what it vendors, not an installed or
importable package. `msgpack` additionally exists only as inert vendored *license* text
(`pip-*.dist-info/licenses/src/pip/_vendor/msgpack`), never as executable code. The running
`uvicorn app.main:app` process never invokes pip, setuptools, or msgpack at runtime. Trivy's Python
package scanner is parsing pip's own vendoring manifest as if it listed installed dependencies —
none of the three referenced versions are present as reachable code in this image.

**Conclusion: none of the three findings is release-blocking.** This is a base-image (`python:3.13-
slim`'s bundled `pip`) characteristic, not something this project's own dependency pins control.

## Known limitations (reviewed, not release blockers)

- gRPC/protobuf calls (Quarkus) — unsupported.
- Kafka topic/subscription semantics (Quarkus) — unsupported.
- `messaging.operation` legacy-attribute recognition gap (Quarkus) — insufficient evidence, deferred.
- PostgreSQL/database dependencies (Airflow) — unsupported.
- Airflow Execution API caller identity — unresolved by design.
- Airflow runtime-role / Celery messaging identity — unresolved, deferred.
- Pre-1.0: Canonical Model, REST API, Graph Schema, Adapter SPI, configuration format not yet
  guaranteed stable.

None represents an incorrect supported claim.

## Release blockers (spec §29)

| Blocker | Present? | Evidence |
|---|---|---|
| Active package version is not `0.3.0` | No | `pyproject.toml`/`uv.lock` both `0.3.0` |
| `uv.lock` inconsistent with project metadata | No | One-line diff, no re-resolution |
| Final tag differs from GO candidate | N/A pending | `v0.3.0` not yet tagged — this record names the candidate that tag must resolve to |
| Candidate changed after qualification | No | This record is for `eeff933`; any later change reopens this gate |
| RC or release provenance ambiguous | No | Full chain: candidate SHA → RC tag → GHCR digest → workflow run, all cross-verified above |
| Release evidence links broken or materially stale | No | All links in this record and the `v0.3.0-rc.2` prerelease verified `200` |
| Material `INCORRECT_SUPPORTED` finding | No | 0 in both systems, both runs |
| Wrong relation direction/identity/runtime status/evidence | No | Byte-identical to I4.4's own frozen, reviewed results |
| Unsupported mechanism represented as supported | No | Unchanged from I4 |
| Ground truth derived from AIP output | No | `expected.yaml` frozen since I2/I3, untouched |
| Unresolved fundamental redesign required before v0.4 | No | I4's `canonical-redesign-gate.md`: `NO` |
| Required Quarkus/Airflow evidence bound to another candidate | No | Freshly bound to `eeff933`, this record |
| Real-system revalidation failure after reopened gate | No | Both systems pass, byte-identical |
| Evaluation below 10/10 | No | 10/10, twice |
| Nondeterministic qualifying result | No | Byte-identical throughout |
| Fresh checkout cannot qualify | No | WP1 above |
| Quick Start fails | No | WP4/§19 above |
| Required CI/CodeQL/dependency audit fails | No | All green at `eeff933` |
| RC or final GHCR artifact cannot be pulled | No | Both `rc.2` and `latest` pulled anonymously |
| Anonymous pull fails | No | Confirmed |
| Container unexpectedly runs as root | No | `uid=1000(app)` |
| Health/Neo4j/import smoke fails | No | All PASS, both local-build and GHCR-pulled image |
| Unexpected duplicate publication workflows | No | Exactly one Docker workflow run for `v0.3.0-rc.2` |
| Unreviewed release-blocking vulnerability | No | Trivy review above — all three findings dispositioned, none blocking |

```text
Critical semantic errors = 0
Release blockers        = 0
```

## Not executed / not applicable

- **Final `v0.3.0` tag/release/tagged-source verification** (spec §24-26) — not applicable to I5.2;
  `v0.3.0` does not exist as a tag yet. That is I5.3, after GO, held for explicit confirmation.
- **Post-release verification** (spec §27) — not applicable until after publication.

## Recommendation

Every mandatory GO condition in spec §23.1 is satisfied against candidate `eeff933`:

```text
I4 handoff complete:                YES
candidate identity frozen:          YES (eeff933da3a8f1e5fc6749cb9a77bbf2e5123fd1)
active package version = 0.3.0:     YES
uv.lock consistent:                 YES
public documentation accurate:      YES (I5.1, PRs #56-#58)
release notes prepared, links valid: YES (draft v0.3.0-release-notes.md, categorized link plan)
clean-checkout source qualification: PASS
unit/integration tests:              PASS (508 + 160)
evaluation = 10/10 twice, deterministic: PASS (byte-identical)
real-system evidence binds to candidate: PASS (fresh, byte-identical to I4.4 baseline)
Quick Start:                         PASS
CI and CodeQL green:                 YES
dependency audit green:              YES
RC GHCR artifact verified:           YES
no unreviewed release-blocking security issue: YES (3 findings reviewed, none blocking)

critical semantic errors:            0
release blockers:                    0
```

**Recommendation: GO**, naming `eeff933da3a8f1e5fc6749cb9a77bbf2e5123fd1` as the exact commit
eligible for the final `v0.3.0` tag.

Per spec §9, this qualification applies only to this exact commit — if any source, dependency lock,
profile, ground truth, or comparator changes afterward, the affected gates reopen and this record no
longer applies to the changed commit.

## Decision

**Pending.** Per this project's established practice (see `v0.2.0-go-no-go.md`'s "decided by the
repository owner"), the GO decision itself — authorizing the final `v0.3.0` tag and publication — is
the repository owner's call, not something this record presumes on their behalf. This document
records that every technical condition for GO is met; it does not itself constitute the decision.

Awaiting explicit confirmation to proceed to I5.3 (cut `v0.3.0`, publish the GitHub Release, verify
the published artifact).

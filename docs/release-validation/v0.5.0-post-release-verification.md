# v0.5.0 Post-Release Verification

This is the published-artifact verification required by I6
[§23](../specifications/0.5.0/i6-release-candidate-publication-and-post-release-verification.md)
(Draft 0.3), for Slice 5. It checks the actual published source, GitHub Release, GHCR artifact and
security state, independently of the pre-publication evidence. The merge commit of the PR that
adds this record is `POST_RELEASE_COMMIT_SHA`. Per §4, the record does not cite its own commit.

```text
terminal outcome = SHIPPED_VERIFIED   (effective when this record is committed; §23, §27.3)
post-release blockers = 0
```

## Identities

| Identity | Value |
|---|---|
| `RELEASE_CANDIDATE_SHA` | `8f63c2369084df9c57535f9112fe2dfa17154505` (`rc.1`, the #264 merge) |
| `EVIDENCE_COMMIT_SHA` | `fa8a72bc542d0aa774b4f849f162033f672e6a3f` ([`v0.5.0-release-readiness.md`](v0.5.0-release-readiness.md), #265) |
| `DECISION_COMMIT_SHA` | `069944b949abc473c848dcb3e00ae74a8fce1f5b` ([`v0.5.0-publication-decision.md`](v0.5.0-publication-decision.md), #266, **GRANTED**) |
| `FINAL_TAG_TARGET_SHA` | `8f63c2369084df9c57535f9112fe2dfa17154505`: the annotated tag `v0.5.0` (tag object `5631b9e225661bbaeae35c8c999e5b4544383fba`) dereferences to the candidate, read with `git ls-remote` against GitHub |
| GitHub Release | [`v0.5.0`](https://github.com/michaelegner/architecture-intelligence-platform/releases/tag/v0.5.0), "AIP v0.5.0", id `RE_kwDOUFwvUc4XrwzU`, published **2026-09-26T18:25:00Z**, not a draft, not a prerelease |
| `FINAL_RELEASE_WORKFLOW_RUN_ID` | `36262460652`, attempt `1`: `docker.yml`, event `release`, head `8f63c23…`, `success` (18:25:02Z to 18:26:44Z) |
| `FINAL_IMAGE_DIGEST` | `sha256:f3c5fdc3691cc10f1de3a190c83e4749c4aadd0c37bf7d0b5385c99de38f9b65` (`ghcr.io/michaelegner/architecture-intelligence-platform`, tagged `v0.5.0` and `latest`) |

The raw evidence is in [`v0.5.0-artifacts/final/`](v0.5.0-artifacts/final/).

## Release workflow attempts (§17.1)

| Run | Attempt | Conclusion | Pushed digest |
|---|---|---|---|
| `36262460652` | 1 | `success` | `sha256:f3c5fdc3…9b65` |

There were no other runs or attempts. Every §8 step succeeded:
- build and push;
- **Record pushed digest**, which validated `^sha256:[0-9a-f]{64}$`;
- the SARIF scan and its upload;
- the full HIGH/CRITICAL report;
- the SBOM;
- **Retain release security evidence**.

That step's own log records `DIGEST: sha256:f3c5fdc3…9b65`, `RUN_ID: 36262460652`,
`RUN_ATTEMPT: 1` and `REF_NAME: v0.5.0` (`security/record-pushed-digest-step.txt`). The workflow
runs from the tagged commit, so this was the first real execution of the §8 changes (readiness
finding R5), and it behaved as specified.

## Final SBOM and security disposition (§19)

- **Evidence:** the artifact `release-security-v0.5.0-sha256-f3c5…` (artifact id `10912538907`)
  was downloaded from the run.
- **SBOM:** `sbom.cdx.json` is CycloneDX 1.6 with 1223 components, SHA-256
  `412dcac04ca99ff5495ed89fc3347a52c64ca2b57709f6abff4b2d0d6d27d7a3`. Its subject is
  `ghcr.io/…@sha256:f3c5fdc3…9b65`, which equals `FINAL_IMAGE_DIGEST`.
- **Full report:** `trivy-full-report.json` (Trivy v0.70.0, `ignore-unfixed: false`), SHA-256
  `612bcea9f421cf2acc137b3cfb6a8b137c2c912c810926fa7ef01bda644ac77e`. Its `ArtifactName` and
  `RepoDigests` are `…@sha256:f3c5fdc3…9b65`.
- **Code scanning on `refs/tags/v0.5.0`:** 3 open alerts, all from the Trivy SARIF upload
  (`security/code-scanning-tag.json`):
  - `msgpack` 1.1.2, GHSA-6v7p-g79w-8964, HIGH;
  - `setuptools` 70.3.0, CVE-2025-47273, HIGH;
  - `setuptools` 70.3.0, CVE-2026-59890, MEDIUM.

  CodeQL does not run on tags. Source CodeQL was established at the exact candidate SHA
  (readiness record, CI table).

**Findings: 46 HIGH, 0 CRITICAL.** This is exactly the candidate image's set: 0 new and 0 gone,
compared finding by finding.

| Origin | Findings | Fix available | Final disposition |
|---|---|---|---|
| Debian base-image OS packages (`util-linux` family, `ncurses`, `libsystemd0`/`libudev1`, `libacl1`, `perl-base`) | 44 HIGH | none (43 `affected`, 1 `fix_deferred`) | `KNOWN_LIMITATION`, reviewed and not release-blocking |
| `msgpack` 1.1.2 and `setuptools` 70.3.0, vendored in the base image's system `pip` 26.2.1 (`pip/_vendor/vendor.txt`); not in the AIP environment, and never run by AIP | 2 HIGH, plus the 1 MEDIUM tag alert | upstream | `KNOWN_LIMITATION`, reviewed and not release-blocking |

The repository owner decided this final-image disposition on 2026-09-26, bound to
`FINAL_IMAGE_DIGEST` and run `36262460652`. No finding is in an AIP dependency.
**Unresolved release-blocking security findings: 0.**

## Anonymous digest pull (§20)

| Check | Result |
|---|---|
| Local copy of the digest before the pull | none |
| `docker logout ghcr.io` | "Removing login credentials for ghcr.io". Afterwards, `~/.docker/config.json` `auths` holds no `ghcr.io` entry and has no `ghcr.io` credHelper, and the credential store lists 0 `ghcr` entries. |
| `docker pull ghcr.io/…@sha256:f3c5fdc3…9b65` (2026-09-26T18:32:30Z) | succeeded; `RepoDigests = ["ghcr.io/…@sha256:f3c5fdc3…9b65"]` |
| Independent anonymous registry check | An anonymous `ghcr.io/token` fetch followed by a manifest GET by digest returns `HTTP/2 200` with `docker-content-digest: sha256:f3c5fdc3…9b65` |
| Pulled digest equals `FINAL_IMAGE_DIGEST` | **yes** |

## Published-image golden path (§20)

`RELEASE_CANDIDATE_SHA=8f63c23… examples/release-golden-path/run.sh ghcr.io/…@sha256:f3c5fdc3…9b65
<out>` was run **from the tagged-source clone** (§21), against the pulled image only; no local build
was involved. It exits 0. The results are in `golden-path/`.

| Phase | Checks passed |
|---|---|
| `demo` | 12 / 12 |
| `pubsub` | 8 / 8 |
| `k8s-agree` | 6 / 6 |
| `k8s-conflict` | 5 / 5 |
| `k8s-unresolved` | 5 / 5 |

In every phase, `run-identity` shows three things:
- the running container's image equals the pulled `FINAL_IMAGE_DIGEST`;
- `AIP_BUILD_REVISION` equals `8f63c23…`;
- the user is `app`.

**The answers carry `producer = {name: architecture-intelligence-platform, version: 0.5.0,
build_revision: 8f63c2369084df9c57535f9112fe2dfa17154505}` and `schema_version "0.5"`.**

The §20 checks break down as follows:
- **Data coverage:** COMPLETE in every phase, before and after the reads. The expected OpenAPI,
  AsyncAPI Queue, Topic/Subscription, Kubernetes and `DEPLOYED_AS` outcomes are present, and runtime
  qualification is present.
- **Identity outcomes:** the `UNRESOLVED` and `CONFLICT` cases are preserved, with no guessed
  association.
- **Reads:** evidence drill-down and same-snapshot continuity hold. The REST and negotiated-MCP
  workflows run with exactly three tools, and a disconnect and reconnect succeed.
- **Zero writes:** the revision fence is unchanged across every public read.

## Tagged-source verification (§21)

`git clone --branch v0.5.0 --depth 1 https://github.com/michaelegner/architecture-intelligence-platform.git`,
into a fresh directory:

| Check | Result |
|---|---|
| `HEAD` | `8f63c2369084df9c57535f9112fe2dfa17154505`, equal to `RELEASE_CANDIDATE_SHA` |
| Working tree | clean, before and after the checks |
| Package version | `0.5.0` |
| `uv sync --locked` | succeeded |
| Release schemas | `schemas/architecture_intelligence/v0.5` (tree `02db433c…`) and `schemas/import/v0.5` (tree `953b15e5…`), both equal to the readiness record |
| Golden-path profile | present; `sha256sum -c SHA256SUMS`: all 51 files OK |
| MCP tools | exactly 3: `get_architecture_drift`, `get_evidence`, `get_service_dependencies` |
| `pytest tests/unit/test_release_version_consistency.py` | 7 passed |
| `python -m evaluation answers --candidate-sha 8f63c23…` | PASS, 23/23 |
| Committed report at the tag | `candidate_sha a906a58…` (pre-refresh). Expected and disclosed (§10.3, §21), so not a finding. |

## GitHub Release verification (§22)

| Check | Result |
|---|---|
| Tag, and the tag target | `v0.5.0`, which dereferences to `8f63c23…` |
| Draft / prerelease | no / no |
| Body identity | byte-identical to the reviewed rendering of the candidate's `v0.5.0-release-notes.md`, SHA-256 `e977067109252e43cb0e3da69667caaf3a89a50a3e4cf03adab007e751812f1d` (`release/published-body.md`). The rendering removed the release-candidate status paragraph and pinned every link to `v0.5.0`. |
| Internal or draft text; unresolved placeholders | none (scanned for `TODO`, `TBD`, `${`, `<PLACEHOLDER>`, `SHALL`, `Draft`, and local paths) |
| Capability claims and limitations | unchanged from the candidate notes. Those were verified claim by claim against contract code and the completion records in #264, and they do not contradict the I1-I5 limitations. |
| Links | 15 unique links, all pinned to `/blob/v0.5.0/` or `/tree/v0.5.0/`; **15/15 return HTTP 200** (`release/link-status.txt`) |

The release API's `target_commitish` shows `main`. That field is not the release identity: the
release was created against the existing tag with `gh release create --verify-tag`, and the tag
dereferences to the candidate.

## Known limitations

These are unchanged from the published release notes and the readiness record:
- Kubernetes is `OFFLINE_ONLY`.
- One Service per Workload, and no locality semantics.
- The Pub/Sub scope exclusions.
- F3: schema composition is uninterpreted. F5: no diagnostic for un-enumerated files. F8: the
  object identity is missing from unsupported-Kubernetes diagnostics.
- Snapshot identity depends on the root path.
- The real-target coverage limits.
- The v0.4.2-only client matrix.
- The pre-refresh evaluation report at the tag.
- The readiness findings R1-R5. R1 and R2 are re-confirmed above for the final image. R5 is
  resolved: the §8 workflow ran as specified.

## Result

```text
tag v0.5.0 -> RELEASE_CANDIDATE_SHA                   PASS
GitHub Release published, verified                    PASS
release workflow identity recorded (1 attempt)        PASS
FINAL_IMAGE_DIGEST recorded                           PASS
SBOM / security disposition bound to final digest     PASS  (owner disposition; 0 blockers)
anonymous pull by digest                              PASS
producer.version = 0.5.0                              PASS
producer.build_revision = RELEASE_CANDIDATE_SHA       PASS
published-image golden path                           PASS  (5/5 phases, 36/36 checks)
tagged-source verification                            PASS
GitHub Release verification                           PASS
post-release blockers                                 0
```

**AIP v0.5.0 is `SHIPPED_VERIFIED`:** the exact qualified release candidate was published, the
tagged source and final GHCR artifact identify that candidate, the final artifact passed the v0.5
golden path and security disposition, and every known unsupported, unresolved, deferred, or
coverage-limited case remains explicit.

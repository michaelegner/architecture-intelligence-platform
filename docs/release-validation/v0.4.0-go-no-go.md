# `v0.4.0` Release Readiness — GO / NO-GO

**Date:** 2026-09-07
**Repository:** `michaelegner/architecture-intelligence-platform`
**Prepared per:** [`i4-release-candidate-publication-and-verification.md`](../specifications/0.4.0/i4-release-candidate-publication-and-verification.md)
(I4.2 — Exact-Candidate and RC Qualification)

Every item below is classified: `VERIFIED`, `KNOWN_LIMITATION`, `NOT_EXECUTED`, `NOT_APPLICABLE`,
or `BLOCKED`. Per spec §35, **I4.2 does not decide GO/NO-GO** — that is I4.3's job. This record
states every *technical* precondition's status only; see "Recommendation" below.

## Candidate

| | |
|---|---|
| Candidate commit | `a906a5895f803287aec973e9e3678655fd8c0f8a` (I4.1, PR #87 squash-merge; tree-identical to the CI-verified pre-squash branch commit `2945e38b4c5e48957dd6ef06e40f10a60a2e049c`) |
| RC tag | `v0.4.0-rc.1` (`git rev-parse v0.4.0-rc.1^{commit}` re-dereferenced for this record — matches) |
| Package version | `0.4.0` (`pyproject.toml`, `uv.lock` consistent — one-line lock diff, no dependency re-resolution) |
| Dependency-lock SHA-256 | `b67974c65faf78d5fce1e55fb5ba39eb7852bf3ed28eb603b679195ac37c8f4e` |
| Schema hashes | `architecture-answer.schema.json` `94af46cd...`, `drift-answer.schema.json` `d81f1ab0...`, `evidence-answer.schema.json` `08cd5a64...` (full hashes in source qualification section) |
| Superseded candidates | `4918dbd2ec8719c2d72a7a911abaa90f8f16be48` and `0f45f5bbc23715f3e4575817a43bae499e14ff12` — pre-I4.1/pre-I3.4-fix intermediate candidates, immutable, unmoved, superseded during PR review before this candidate was frozen |

## Environment (applies to every check below unless stated otherwise)

```text
Host:                 Linux 5.15.153.1-microsoft-standard-WSL2 x86_64
Docker:                29.7.2
Compose:               v5.3.1 (Compose v2 spec)
Python (venv):         3.13.14
uv:                    0.11.24
Qualification method: isolated `git clone` at the exact candidate SHA for source qualification;
                       the actual published GHCR image (anonymous pull) for RC artifact
                       qualification — never the ambient development checkout or a local build
                       standing in for the published artifact
```

## Source qualification (spec §16/§17)

Isolated fresh `git clone` + `git checkout a906a5895f803287aec973e9e3678655fd8c0f8a`, verified
`git rev-parse HEAD` matches and `git status --porcelain` empty before any command ran.

```text
uv sync --locked                                              exit 0   Resolved 65 packages
uv run ruff check .                                           exit 0   "All checks passed!"
uv run ruff format --check .                                  exit 0   "233 files already formatted"
uv run pytest tests/unit -q                                   exit 0   828 passed
uv run pytest tests/integration -q                             exit 0   244 passed
uv run python -m evaluation run                                exit 0   10/10 PASS (below)
uv run python -m evaluation answers --candidate-sha a906a58... exit 0   23/23 PASS (below)
uv run --with pip-audit pip-audit                              exit 0   "No known vulnerabilities found"
```

| Check | Classification | Evidence |
|---|---|---|
| `uv sync --locked` | `VERIFIED` | Resolved/installed cleanly, no lock drift |
| `ruff check .` | `VERIFIED` | Exit 0, "All checks passed!" |
| `ruff format --check .` | `VERIFIED` | Exit 0, 233 files already formatted |
| Unit tests | `VERIFIED` | Exit 0, 828 passed |
| Integration tests | `VERIFIED` | Exit 0, 244 passed |
| `pip-audit` | `VERIFIED` | Exit 0, no known vulnerabilities |
| Relation-facts evaluator (spec §17) | `VERIFIED` | Exit 0, `10/10` PASS, 0 missing/unexpected/forbidden/wrong-status/evidence-errors |
| Architecture-answer evaluator (spec §17/§18), 2 clean-state passes | `VERIFIED` | `result=PASS`, `run_count=2`, `semantic_outputs_identical=true`, `summary={scenarios:23, passed:23, failed:0}` |
| Cross-tool invariants | `VERIFIED` | `dependency_to_drift=PASS`, `drift_to_evidence=PASS`; `service_to_mcp`/`insufficient_evidence_qualification` explicitly `QUALIFIED_EXTERNALLY` (not fabricated as evaluator-local PASS), per spec §17 |
| Frozen real-system-derived qualification (spec §20) | `VERIFIED` | `quarkus-super-heroes` PASS (source hash `656446cd79c4cefec8f1ac0124fbb6b34e993704`), `apache-airflow` PASS (source hash `8891289baa9facaf70a0cc0c6b9b2e0fdd9c838a`) — no fresh live rerun, per spec §20's default |
| Candidate-bound evaluation artifact SHA-256 | `VERIFIED` | `93e1d308418aea5c2c5550984384b800b0ca1cf6ec4250ef267a2c7371f55330` (per spec §18's loop-avoidance discipline — this record cites it as evidence *about* the candidate, not committed at candidate-freeze time) |
| Evaluation requires no LLM key | `VERIFIED` | `OPENAI_API_KEY` unset throughout |
| Schema freeze | `VERIFIED` | All three committed schemas match generated schemas (part of the unit-test run above); hashes unchanged from I4.1 |

```text
Architecture-answer evaluator: Scenarios: 23   Passed: 23   Failed: 0
Relation-facts evaluator:      Scenarios: 10   Passed: 10   Failed: 0
```

## Independent-client and hero qualification (spec §19)

`tests/integration/test_mcp_independent_client_golden_path.py` (part of the 244 integration tests
above) covers the real-`uvicorn`/production-app-wiring/no-in-process-ASGI-shortcut requirement —
no second MCP test framework was introduced, per spec §19.

The documented hero demo (`examples/runtime-demo/hero-demo.md`) was additionally run twice from
separate clean `docker compose down -v` states, at this exact candidate, diffing the **complete**
`structuredContent` of both `get_architecture_drift` and `get_evidence` (not only qualifications):

| Check | Classification | Evidence |
|---|---|---|
| Hero demo run 1 → run 2, full `structuredContent` | `VERIFIED` | Byte-for-byte identical — `snapshot_id`, `model_revision`, `observation_context`, `claims`, `evidence_refs`, evidence `records` (including `sample_trace_ids`/`first_seen`/`last_seen`) all identical |
| `LegacyPricingService` = `OBSERVED_ONLY` | `VERIFIED` | Both runs |
| `unused-q` = `NOT_OBSERVED_IN_WINDOW` | `VERIFIED` | Both runs |
| `ProductService`/`payment-q` excluded from drift | `VERIFIED` | Both runs — `CONFIRMED`, correctly absent from `get_architecture_drift` |
| No `OPENAI_API_KEY` required | `VERIFIED` | Unset throughout |

## Public usage / local candidate image qualification (spec §21)

Local image built from source at the exact candidate with `AIP_BUILD_REVISION` explicitly set
(not relying on git-fallback resolution):

| Check | Classification | Evidence |
|---|---|---|
| `docker build --build-arg AIP_BUILD_REVISION=a906a58...` | `VERIFIED` | Built cleanly |
| Container runs as non-root | `VERIFIED` | `docker exec ... id` → `uid=1000(app) gid=1000(app)` |
| `GET /health` | `VERIFIED` | `{"status":"ok"}` |
| `GET /health/neo4j` | `VERIFIED` | `{"status":"ok"}` |
| `POST /api/import` | `VERIFIED` | All 4 bundled `examples/` services imported, 0 errors |
| `tools/list` returns exactly 3 tools | `VERIFIED` | `get_architecture_drift`, `get_evidence`, `get_service_dependencies` (lexicographic) |
| `producer.version` | `VERIFIED` | `0.4.0` |
| `producer.build_revision` | `VERIFIED` | `a906a5895f803287aec973e9e3678655fd8c0f8a` — matches candidate exactly |

## RC GHCR artifact qualification (spec §22/§23)

| Check | Classification | Evidence |
|---|---|---|
| RC tag points to exact candidate | `VERIFIED` | `git rev-parse v0.4.0-rc.1^{commit}` → `a906a5895f803287aec973e9e3678655fd8c0f8a` |
| Release-triggered Docker workflow ran exactly once for `v0.4.0-rc.1` | `VERIFIED` | [run 34112182667](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/34112182667), event `release`, `head_sha=a906a58`, conclusion `success` |
| Tagged image pull | `VERIFIED` | `docker pull ghcr.io/michaelegner/architecture-intelligence-platform:v0.4.0-rc.1` succeeded |
| Digest recorded | `VERIFIED` | `sha256:834aff72ea7d4f3e1eea78a9ebc8bc69ca7dd7e3dd7b08b77699e95fb807e60e` |
| Anonymous pull | `VERIFIED` | `docker logout ghcr.io` run before pulling; no credentials used for either pull |
| Container runs as non-root (published image) | `VERIFIED` | `docker exec ... id` → `uid=1000(app) gid=1000(app)` |
| `v0.4.0-rc.1` digest == `latest` digest | `VERIFIED` | Both `sha256:834aff72...` — expected side effect of this repo's `docker.yml` (also occurred for `v0.3.0-rc.1`/`rc.2`); corrected again at final GO (I4.3/I4.4), not a blocker |

## Published RC MCP golden path (spec §24)

Full golden path executed against the **published, anonymously-pulled** image (not a local build),
against a fresh Neo4j:

```text
1. docker pull (anonymous)                                         VERIFIED
2. GET /health                                                       VERIFIED  {"status":"ok"}
3. GET /health/neo4j                                                 VERIFIED  {"status":"ok"}
4. POST /api/import                                                  VERIFIED  4 services imported
5. seed deterministic frozen telemetry (seed_frozen_evidence.py      VERIFIED  8 resource span
   semantics, sent directly to the published container's /v1/traces)           blocks, fixed
                                                                                2026-08-26T12:00:00Z
6. tools/list                                                        VERIFIED  exactly 3, correct
                                                                                order
7. get_architecture_drift(service:order-service)                    VERIFIED  LegacyPricingService=
                                                                                OBSERVED_ONLY,
                                                                                unused-q=
                                                                                NOT_OBSERVED_IN_WINDOW
8. get_evidence (same snapshot_id)                                   VERIFIED  3 records resolved,
                                                                                0 missing
9. producer.version / producer.build_revision                       VERIFIED  0.4.0 /
                                                                                a906a58...
```

## CI, CodeQL, and Trivy qualification (spec §21/§25)

All checks queried directly against the exact candidate SHA (`a906a58`) via the GitHub Checks API
— never inferred from a later PR-head badge.

| Check | Classification | Evidence |
|---|---|---|
| CI — `lint + test` | `VERIFIED` | [run 34107166335](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/34107166335/job/101694720715) |
| CI — `dependency security scan (pip-audit)` | `VERIFIED` | [run 34107166335](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/34107166335/job/101694721209) |
| CodeQL — `analyze (python)` | `VERIFIED` | [run 34107166376](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/34107166376/job/101694720773) |
| CodeQL — `analyze (actions)` | `VERIFIED` | [run 34107166376](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/34107166376/job/101694721025) |
| Docker — `build, scan, and push` | `VERIFIED` | [run 34112182667](https://github.com/michaelegner/architecture-intelligence-platform/actions/runs/34112182667), triggered once by the `v0.4.0-rc.1` release |

### Trivy review (spec §25's explicit review requirement — non-blocking exit does not mean auto-approved)

**Zero** open code-scanning alerts of any kind exist in the repository at review time
(`gh api .../code-scanning/alerts` — 1 total alert, a historical CodeQL finding fixed 2026-08-27;
0 from this or any Trivy scan). The Docker workflow's Trivy step (CRITICAL/HIGH, `ignore-unfixed`)
ran to completion and its SARIF was uploaded and validated without producing any alert.

```text
Known critical release-blocking security findings = 0
Unreviewed release-blocking image findings        = 0
```

## Known limitations (`KNOWN_LIMITATION`, not release blockers)

- Direct dependencies only — no transitive dependency traversal.
- Current state only — no historical/point-in-time snapshots, no snapshot diffing.
- No generic Cypher/graph query surface through any tool.
- MCP is exposed for a local/trusted-network posture — not hardened for direct public-internet
  exposure, no authentication/authorization, no multi-tenancy.
- All `v0.3.0` limitations still apply unchanged (frozen, reused evidence): gRPC/protobuf calls and
  Kafka topic/subscription semantics (Quarkus) unsupported; PostgreSQL/database dependencies
  (Airflow) unsupported; Airflow Execution API caller identity unresolved by design; Airflow
  runtime-role/Celery messaging identity unresolved, deferred; a legacy OpenTelemetry
  messaging-attribute recognition gap (Quarkus) insufficient evidence, deferred.
- Pre-1.0: Canonical Model, REST/MCP API surface, Graph Schema, Adapter SPI, and configuration
  format are not yet guaranteed stable.

None represents an incorrect supported claim.

## Release blockers (spec §26)

Every named blocker checked explicitly — all `VERIFIED` absent unless noted.

### Semantic integrity

| Blocker | Classification | Evidence |
|---|---|---|
| Unexpected architecture claim | `VERIFIED` absent | 0 unexpected claim IDs across all 23 evaluator scenarios |
| Wrong qualification | `VERIFIED` absent | 0 field mismatches across all 23 scenarios |
| Dropped limitation | `VERIFIED` absent | Evaluator asserts limitation presence per scenario; 0 failures |
| Broken evidence reference | `VERIFIED` absent | 0 broken evidence refs across all scenarios; hero demo evidence drill-down resolved fully |
| Snapshot/context substitution | `VERIFIED` absent | `drift-stale-snapshot`/`drift-missing-context` scenarios PASS |
| Guessed unresolved identity | `VERIFIED` absent | Airflow unresolved-identity findings remain explicit limitations, unchanged from frozen evidence |
| Non-observation represented as absence | `VERIFIED` absent | `NOT_OBSERVED_IN_WINDOW` semantics unchanged, proven by evaluator + hero demo |
| MCP/direct-service semantic disagreement | `VERIFIED` absent | `service_to_mcp` `QUALIFIED_EXTERNALLY` via `test_mcp_drift_scenario_parity.py`; hero demo confirms live |

### Scope and safety

| Blocker | Classification | Evidence |
|---|---|---|
| MCP adapter graph logic/direct graph access | `VERIFIED` absent | Unchanged since I2.1/I3.2 boundary tests (part of unit suite) |
| Generic query exposure | `VERIFIED` absent | No such surface exists |
| Write-capable tool | `VERIFIED` absent | Read-only boundary tests pass; MCP graph writes = 0 |
| LLM required for correctness | `VERIFIED` absent | No `OPENAI_API_KEY` used anywhere in this qualification |
| Fourth public tool | `VERIFIED` absent | `tools/list` returns exactly 3, both locally and on the published image |
| Second transport | `VERIFIED` absent | Single `/mcp` Streamable HTTP endpoint, unchanged |
| New unqualified model/discovery semantics | `VERIFIED` absent | No `app/**` changes since I3.4 |

### Determinism and usability

| Blocker | Classification | Evidence |
|---|---|---|
| Derived expected ground truth | `VERIFIED` absent | Fixtures unchanged since I1.4/I3.3, not regenerated here |
| Evaluation mismatch | `VERIFIED` absent | 23/23 PASS |
| Repeat-run semantic difference | `VERIFIED` absent | `semantic_outputs_identical=true`, two passes |
| Independent-client failure | `VERIFIED` absent | Integration suite + hero demo both pass |
| Evidence drill-down failure | `VERIFIED` absent | Same-snapshot resolution succeeds, locally and on the published image |
| Hero clean-state failure | `VERIFIED` absent | Two independent clean-state runs, byte-identical |
| Published RC MCP golden-path failure | `VERIFIED` absent | Full golden path passed against the published image |

### Release quality

| Blocker | Classification | Evidence |
|---|---|---|
| Package/lock version mismatch | `VERIFIED` absent | Both `0.4.0`, one-line lock diff |
| Candidate changes after qualification | `VERIFIED` absent | This record is for `a906a58`; any later change reopens this gate |
| CI/CodeQL/pip-audit failure | `VERIFIED` absent | All green, run IDs above |
| Published image provenance mismatch | `VERIFIED` absent | `producer.build_revision` on the published image = exact candidate SHA |
| Published RC not publicly pullable | `VERIFIED` absent | Anonymous pull succeeded |
| Published RC MCP smoke failure | `VERIFIED` absent | Full golden path passed |
| Unreviewed critical security issue | `VERIFIED` absent | Zero findings, Trivy review above |

```text
Incorrect supported architecture claims = 0
Unexpected tool claims                  = 0
Broken evidence references              = 0
Write-capable MCP operations            = 0
Release blockers                        = 0
```

## Not executed / not applicable

- **Final `v0.4.0` tag/release/tagged-source verification** (I4.3/I4.4) — `NOT_APPLICABLE`;
  `v0.4.0` does not exist as a tag yet. Held for explicit confirmation after GO.
- **Post-release verification** — `NOT_APPLICABLE` until after publication.
- **Fresh Quarkus/Airflow live execution** — `NOT_EXECUTED` deliberately; spec §20 requires none by
  default, and nothing in I4 changed architecture/evaluation semantics or fixture derivation.

## Recommendation

Every mandatory *technical* GO condition in spec §28 has been `VERIFIED` against candidate
`a906a58`:

```text
candidate frozen:                          VERIFIED (a906a5895f803287aec973e9e3678655fd8c0f8a)
package version = 0.4.0:                   VERIFIED
uv.lock root version = 0.4.0:              VERIFIED
schemas qualified:                         VERIFIED
unit/integration tests PASS:               VERIFIED (828 + 244)
relation-facts evaluator PASS:             VERIFIED (10/10)
architecture-answer evaluator PASS:        VERIFIED (23/23)
two answer passes identical:               VERIFIED (semantic_outputs_identical=true)
cross-tool invariants qualified:           VERIFIED
Quarkus/Airflow-derived qualification PASS: VERIFIED
hero deterministic:                        VERIFIED (byte-identical, twice)
CI PASS:                                   VERIFIED
CodeQL PASS:                               VERIFIED
pip-audit PASS:                            VERIFIED
RC tag -> exact candidate:                 VERIFIED
RC Docker workflow PASS:                   VERIFIED
RC image publicly pullable:                VERIFIED
RC image non-root:                         VERIFIED
RC producer build revision = candidate:    VERIFIED
published RC MCP golden path PASS:         VERIFIED
Trivy reviewed:                            VERIFIED (0 findings)
release blockers = 0:                      VERIFIED

critical semantic errors:                  0
release blockers:                          0
```

**This satisfies every technical precondition for GO. It does not itself constitute GO** — the
decision itself belongs to the repository owner, per this project's own `v0.3.0-go-no-go.md`
precedent (I4.3's job, per spec §35's explicit I4.2/I4.3 split — unlike v0.3.0's I5.2, this
release's I4.2 does not itself decide GO/NO-GO). The candidate/RC identity a GO statement must name
(spec §28) is:

```text
Final candidate SHA:      a906a5895f803287aec973e9e3678655fd8c0f8a
Final RC tag:              v0.4.0-rc.1
Dependency-lock SHA-256:   b67974c65faf78d5fce1e55fb5ba39eb7852bf3ed28eb603b679195ac37c8f4e
RC image digest:           sha256:834aff72ea7d4f3e1eea78a9ebc8bc69ca7dd7e3dd7b08b77699e95fb807e60e
```

Per spec §9, this qualification applies only to this exact commit — if any source, dependency
lock, profile, ground truth, or comparator changes afterward, the affected gates reopen and this
record no longer applies to the changed commit.

## Decision

**`GO`** — decided by the repository owner, 2026-09-07. This decision was recorded in this
separate commit rather than the evidence commit itself, since a commit cannot cite its own SHA as
the spec §28 GO-record revision (the same two-commit discipline this project's own
`v0.3.0-go-no-go.md` established).

Between the evidence commit below and this decision, `git log a906a58..main` was re-checked and
showed no candidate-mutating change — `RELEASE_CANDIDATE_SHA` is unchanged, and every check in
this record still applies.

```text
GO-record revision:          4297131dd82029e08b68e5f28176cad478d030a1 (PR #88 merge — the
                              evidence commit that introduced this file and every check it
                              records; this decision was recorded in a later commit, which is
                              not itself the GO-record revision)
Final candidate SHA:          a906a5895f803287aec973e9e3678655fd8c0f8a
Final RC tag:                 v0.4.0-rc.1
Dependency-lock SHA-256:      b67974c65faf78d5fce1e55fb5ba39eb7852bf3ed28eb603b679195ac37c8f4e
RC image digest:              sha256:834aff72ea7d4f3e1eea78a9ebc8bc69ca7dd7e3dd7b08b77699e95fb807e60e
```

**I4.2 is now closed.** I4.3 is authorized to proceed: cut the immutable `v0.4.0` tag at
`a906a5895f803287aec973e9e3678655fd8c0f8a`, publish the GitHub Release, and confirm the
release-triggered Docker workflow fires exactly once (spec §29) — published-artifact verification
and public status closure remain I4.4's job (spec §30/§32), not this decision's. Per spec §9, this
approval applies only to the exact candidate SHA above — if `main` advances before tagging
(including via this decision commit itself), the tag SHALL still point at this approved SHA
directly, not a newer `main` HEAD, unless that newer commit is explicitly requalified and this
record updated.

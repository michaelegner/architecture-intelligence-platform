# Validation Profile — `<system-id>`

The bounded, reproducible profile actually exercised (I1 §9). Broad enough to challenge AIP's
current semantics; small enough to run repeatably during release qualification.

## Components/processes started

<!-- which services/processes are started -->

## Runtime/deployment mode

<!-- local, containerized, etc. -->

## Broker/executor configuration

<!-- which broker/executor configuration is used, if applicable -->

## Telemetry configuration

<!-- which telemetry is enabled, and how (I1 §10: standard OTel export/OTLP/Collector config only) -->

## Architecture flows exercised

<!-- which architecture flows are exercised -->

## Supported AIP semantics in scope

<!-- which AIP semantics (relation types, runtime statuses) this profile is expected to validate -->

## Known upstream mechanisms out of scope

<!-- which upstream features are intentionally out of scope for this profile -->

## Instrumentation additions

<!-- every instrumentation addition beyond default upstream configuration, documented per I1 §10.
     Adding an AIP-specific architecture manifest after inspecting AIP output, renaming services to
     improve matching, or rewriting messaging topology to fit AIP semantics are NOT allowed here. -->

## Startup procedure

<!-- ordered steps -->

## Traffic/exercise procedure

<!-- ordered steps -->

## Shutdown procedure

<!-- ordered steps, including how to reset upstream/broker/AIP graph/telemetry state to clean
     (I1 §29) -->

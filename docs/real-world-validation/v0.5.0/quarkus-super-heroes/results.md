# Results: `quarkus-super-heroes` (v0.5.0, I5 Slice 5)

This is the qualifying run of the frozen dossier (#240; post-freeze corrections #243 and #246)
against **candidate `174a17c5d0f8be35291032c677585291c330cc0a`**, the #246 merge commit. It ran on
2026-09-24 from the frozen checkout location, with the runbook executed as written. Every raw
record is in `artifacts/`.

## Run identity

| Item | Value | Record |
| --- | --- | --- |
| Candidate SHA | `174a17c5d0f8be35291032c677585291c330cc0a` (clean checkout at the frozen location; checked in steps 2 and 5) | `artifacts/candidate-sha` |
| AIP image | `aip-i5-candidate:174a17c…`, id `sha256:b4deaee8ec3f5f47c561263bbb5f8454ab54f9ecc9568428cf2a7767e582bc49`, built with `--no-cache` from the candidate. The running container's `AIP_BUILD_REVISION` equals the candidate. | `artifacts/aip-image` |
| Service images | Built with `--no-cache` from a fresh clone at `8ea0337`, on the digest-pinned Maven builder and `ubi10/openjdk-25-runtime` base. Every running container was checked against these ids before any import or traffic. | `artifacts/service-images` |
| Third-party images | All run by their frozen digests (`upstream.md`) | `artifacts/compose-images.json` |
| Observation window | `2026-09-24T21:44:48Z` to `2026-09-24T21:44:59Z` (closed when the 3 exercised CALLS were first `CONFIRMED`) | `artifacts/window-start`, `artifacts/window-end` |
| Procedure | The frozen runbook blocks 1-10, extracted verbatim into `artifacts/run.sh`, plus the disclosed deviations in its header: a fixed record directory, the password from a file, the v0.3 readiness helpers, and the window saved to files | `artifacts/run.sh`, `artifacts/run.log` |

The upstream checks passed. The fresh clone is at the pin, the four OpenAPI documents and the
unmodified manifest are byte-identical to the frozen copies, and the namespace-derived copy
regenerated byte-for-byte.

The service images are not byte-reproducible across builds (`upstream.md`). The attempt at
`34067b7` built different image ids from the same recipe.

## Import (runbook step 6; `artifacts/import.json`)

| Source | Result |
| --- | --- |
| `declarations/rest-{fights,heroes,villains,narration}/openapi.yml` | `ACCEPTED` ×4 |
| `declarations/rest-fights/architecture.yaml` | `ACCEPTED` (7 CALLS) |
| `k8s/namespaced/envelope.yaml` | `ACCEPTED_WITH_LIMITATIONS` (111 nodes) |
| `k8s/unmodified/envelope.yaml` | Absent from `sources`, which is how a non-committing run appears through the API |

The top-level `committed` is `false`. That comes only from the designed rejection of the
unmodified bundle; every configured source is its own atomic run. The rejection code
(`K8S_RESOURCE_INVALID`) and the exact frozen limitation list of the namespaced bundle **cannot be
observed**, because the import response carries no diagnostics. That is finding **F2** in
`../finding-ledger.md`.

## Comparison (runbook steps 9-10; `artifacts/actual.yaml`, `artifacts/compare.txt`)

```text
Expected supported facts:      45        (35 PROVIDES + 7 CALLS + 3 DEPLOYED_AS)
Correct:                       45
Missing supported:             0
Incorrect supported:           0
Unsupported constructs:        3         (gRPC, Kafka fights, legacy messaging.operation key)
Forbidden facts proven absent: 4         (2 × queue:fights, 2 × name-only DEPLOYED_AS)
Forbidden facts present:       0
Critical semantic errors:      0
```

- **CALLS.** All seven match exactly. The 3 exercised calls are `CONFIRMED` (declared and
  observed), and the 4 unexercised calls are `NOT_OBSERVED_IN_WINDOW` (declared, not observed).
- **DEPLOYED_AS.** rest-fights, rest-heroes and rest-villains are `RESOLVED_CONFIGURED` with
  `[RESOLVED_CONFIGURED]`. rest-narration and event-statistics never resolve.
- **Scope closure.** No unexpected in-scope fact. No Queue, Topic or Subscription relation for
  Kafka `fights`.

## Manual checks (`ground-truth.md`; `artifacts/manual-checks/`)

These use read-only Cypher, because I2 infrastructure entities are internal-only (no public read)
and diagnostics are not exposed (F2).

| Check | Frozen expectation | Observed |
| --- | --- | --- |
| Kubernetes inventory | 13 Workloads, 13 Kubernetes Services, 1 Ingress | 13 `KUBERNETES_WORKLOAD` (all `Deployment` in `quarkus-super-heroes`), 13 `KUBERNETES_NETWORK_SERVICE`, 1 `KUBERNETES_INGRESS` |
| Claims | 13 `WORKLOAD_EXISTS` and 2 Ingress routes; no selection or ownership claim | 13 `WORKLOAD_EXISTS`; `INGRESS_ROUTES_TO_NETWORK_SERVICE` ui-super-heroes → rest-fights and → ui-super-heroes; nothing else |
| Evidence mode | `DECLARED_MANIFEST` | All 27 contributions are `DECLARED_MANIFEST` |
| Messaging entities | none | none |
| Relations | no interaction fact from Kubernetes | Exactly 35 PROVIDES and 7 CALLS (plus request and response schema edges) |
| Services | the declared four | The declared four, plus `service:grpc-locations` with `discovery_status: OBSERVED_ONLY` and **no relations**. That is finding **F4**. |

## Public surfaces (`../public-surfaces.md`; `artifacts/public-surfaces/`)

| Check | Result |
| --- | --- |
| `tools/list` | Exactly `get_architecture_drift`, `get_evidence`, `get_service_dependencies` |
| Dependencies of `service:rest-fights` (8 claims: 7 CALLS + 1 `DEPLOYED_AS`, resolution `RESOLVED_CONFIGURED`) | Service = REST = negotiated MCP (identical JSON) |
| Drift of `service:rest-fights` (4 claims, the `NOT_OBSERVED_IN_WINDOW` CALLS) | Identical across the three surfaces |
| Evidence drill-down (8 refs requested, 8 records, 0 missing) | Identical across the three surfaces |
| Snapshot | One snapshot on every read: `aip:snapshot:v1:92bfd2f4…` |
| Zero writes | `Q-GRAPH` sha256 before and after = `92f045b3…43f3` |

The parity implementation is `../parity.py`: Service `model_dump(mode="json")`, the REST body and
the MCP `structuredContent` via `tests/integration/independent_mcp_client.py`.

## Lifecycle

The lifecycle results for both targets are in `../lifecycle/results.md`. For this target, steps S0,
L1, L4a, L4b, L6 and R match the frozen ledger. **L2, L5 and L3 failed with HTTP 500.** The
frozen mutation for those steps is invalid (**F7**), and AIP crashes on it instead of rejecting it
(**F1**).

## Aborted attempt (disclosed, non-qualifying)

A first attempt at `34067b7` stopped at runbook step 9. The dossier's bindings document had a
layout the discoverer never enumerates, so the declarations run never committed. #246 corrected it
without changing any expected fact. The records of that attempt are not committed; its test-suite
results and the import symptom are summarized in `../finding-ledger.md` (F5).

# Final-candidate results: `quarkus-super-heroes` (v0.5.0, I5 Slice 7)

This is the I5 §12 revalidation of the frozen dossier against **candidate
`aa04a150965924bd23ecf0125bcf7095a3cf72d9`**, the #257 merge. It includes:
- the Slice 5 corrections #243 and #246;
- the Slice 6 FIXes #254 and #255;
- the lifecycle re-freeze #256 and the result-label freeze #257.

It ran on 2026-09-25 from the frozen checkout location, with the runbook executed as written. Every
raw record is in `artifacts/`. The Slice 5 run of this dossier (`../../quarkus-super-heroes/
results.md`) is left unchanged as the record of that candidate.

## Run identity

| Item | Value | Record |
| --- | --- | --- |
| Candidate SHA | `aa04a150965924bd23ecf0125bcf7095a3cf72d9`. It was a clean checkout at the frozen location, checked in steps 2 and 5. | `artifacts/candidate-sha` |
| AIP image | `aip-i5-candidate:aa04a15…`, id `sha256:5573927999a4b845d11b485c0206063649f4c6fa67aee6e82b4bffc468587a09`, built with `--no-cache` from the candidate. The running container's `AIP_BUILD_REVISION` equals the candidate. | `artifacts/aip-image` |
| Service images | Built with `--no-cache` from a fresh clone at `8ea0337`, on the digest-pinned builder and base images. Every running container was checked against these ids before any import or traffic. They differ from Slice 5's ids, because the service builds are not byte-reproducible (`../../quarkus-super-heroes/upstream.md`). | `artifacts/service-images` |
| Third-party images | All run by their frozen digests | `artifacts/compose-images.json` |
| Observation window | `2026-09-25T13:06:47Z` to `2026-09-25T13:06:54Z` | `artifacts/window-start`, `artifacts/window-end` |
| Procedure | The frozen runbook blocks 1-10. The runbook is unchanged since Slice 5, so the Slice 5 `run.sh` was reused with only its record directory changed (a checked diff). It carries the same disclosed deviations: a fixed record directory, the password from a file, the v0.3 readiness helpers, and the window saved to files. | `artifacts/run.sh`, `artifacts/run.log` |

The upstream checks passed. The fresh clone is at the pin, the four OpenAPI documents and the
unmodified manifest are byte-identical to the frozen copies, and the namespace-derived copy
regenerated byte-for-byte.

## Import (runbook step 6; `artifacts/import.json`, report `aip-import-report/1`)

Since F2 (#254), the report's `runs[]` shows every source result and diagnostic code:

| Run | Status | Source results |
| --- | --- | --- |
| `qsh-v0.5-declarations` | `COMPLETE`, committed | 4 × OpenAPI and `rest-fights/architecture.yaml`, all `ACCEPTED` with no diagnostics |
| `qsh-k8s-namespaced` | `COMPLETE`, committed | `envelope.yaml` `ACCEPTED_WITH_LIMITATIONS` (111 nodes). Its codes are `K8S_RESOURCE_UNSUPPORTED` and `NO_QUALIFIED_POD_MATCH` only. |
| `qsh-k8s-upstream-unmodified` | `PARTIAL`, not committed | `envelope.yaml` `REJECTED_INVALID` with `K8S_RESOURCE_INVALID` |

The top-level `committed` is `false`. That comes only from the designed rejection of the unmodified
bundle, and every configured source is its own atomic run.

**The frozen limitation list (matrix row 3; `ground-truth.md`).** The list is frozen per object: 25
`K8S_RESOURCE_UNSUPPORTED` objects and 13 `NO_QUALIFIED_POD_MATCH` Services, and nothing else.
- **`NO_QUALIFIED_POD_MATCH`:** exact, per object. The report names the 13 frozen Services.
- **`K8S_RESOURCE_UNSUPPORTED`:** exact per kind, not attributable per object. For an unsupported
  kind, the diagnostic pointer names only the file and the kind (`…:v1/ConfigMap`). So the report's
  deterministic deduplication shows six per-kind entries: Secret, ConfigMap, ServiceAccount, Role,
  RoleBinding and Job. Counting the diagnostics of AIP's own discovery over the same input at the
  candidate gives 9 Secret, 10 ConfigMap, 1 ServiceAccount, 1 Role, 3 RoleBinding and 1 Job. That
  is exactly the frozen list's distribution (25), and no other code appears
  (`artifacts/k8s-limitations-per-kind.txt`). The two-target impact test, which passed in the
  suite run at the candidate, asserts the same 25 and 13.

  Which named object each limitation belongs to cannot be observed anywhere. That is finding
  **F8** (`../finding-ledger.md`), which is `DEFER`.

## Comparison (runbook steps 9-10; `artifacts/actual.yaml`, `artifacts/compare.txt`)

```text
Expected supported facts:      45        (35 PROVIDES + 7 CALLS + 3 DEPLOYED_AS)
Correct:                       45
Missing supported:             0
Incorrect supported:           0
Unsupported constructs:        3         (gRPC, Kafka fights, legacy messaging.operation key)
Forbidden facts proven absent: 4         (2 × queue:fights, 2 × name-only DEPLOYED_AS)
Forbidden facts present:       0
Critical semantic errors:      0
```

This is identical to Slice 5:
- **CALLS:** 3 are `CONFIRMED` and 4 are `NOT_OBSERVED_IN_WINDOW`;
- **DEPLOYED_AS:** rest-fights, rest-heroes and rest-villains are `RESOLVED_CONFIGURED`, and
  rest-narration and event-statistics never resolve;
- no Queue, Topic or Subscription relation exists for Kafka `fights`.

## Manual checks (`artifacts/manual-checks/`)

All eleven read-only checks were re-run with the Slice 5 queries, and all eleven outputs are
**byte-identical to Slice 5's**:
- 13 Workloads, 13 Kubernetes Services and 1 Ingress;
- 13 `WORKLOAD_EXISTS` and 2 Ingress routes, all 27 contributions `DECLARED_MANIFEST`;
- no messaging entities;
- exactly 35 PROVIDES and 7 CALLS;
- the declared four Services plus the relation-less `OBSERVED_ONLY` `service:grpc-locations` (F4).

## Public surfaces (`../../public-surfaces.md`; `artifacts/public-surfaces/`)

| Check | Result |
| --- | --- |
| `tools/list` | Exactly `get_architecture_drift`, `get_evidence`, `get_service_dependencies` |
| Dependencies, drift and the evidence drill-down of `service:rest-fights` | Service = REST = negotiated MCP on all reads (`parity-summary.json`: `all_equal: true`, 4 reads) |
| Snapshot | One snapshot on every read: `aip:snapshot:v1:f01a35de…` |
| Zero writes | `Q-GRAPH` sha256 before and after = `aa5cda2d…be11` |

The parity checker is the committed `../../parity.py`, whose behavior is identical to the as-run
Slice 5 copy.

## Lifecycle

The results are in `../lifecycle/results.md`. **All nine steps match the frozen ledger**, including
L2, L5 and L3 on the corrected X (#256) and the frozen L4b/L6 result labels (#257).

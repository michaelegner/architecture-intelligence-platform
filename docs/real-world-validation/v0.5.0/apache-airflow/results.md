# Results: `apache-airflow` (v0.5.0)

There has been no qualifying run yet. The dossier was frozen in I5 Slice 3. Slice 5 records the
following here, per `runbook.md` step 11:
- the candidate SHA and the window;
- the image identities;
- the import response;
- the comparator report;
- the non-comparator checks from `ground-truth.md`.

"""v0.4.1 I3.1 - the committed, reproducible whole-graph snapshot/read-cost benchmark.

Dev/release-qualification tooling only (like `evaluation/`): never imported by `app/`, and never
copied into the production Docker image (see the `Dockerfile`'s `COPY` list).
"""

asyncapi: 2.6.0
info:
  title: ProductService
  version: 1.0.0
channels:
  order-status-q:
    subscribe:
      operationId: receiveOrderStatus
      message:
        $ref: '#/components/messages/OrderStatus'
    x-aip-destination-kind: queue
components:
  messages:
    OrderStatus:
      name: OrderStatus
      payload:
        type: object
x-aip-service-id: service:product-service
servers:
  asb:
    url: amqps://asb.example.com
    protocol: amqp
    x-aip-broker-id: asb
    bindings:
      amqp:
        virtualHost: commerce

asyncapi: '2.6.0'
info:
  title: OrderService
  version: "1.0.0"
channels:
  order-status-q:
    publish:
      operationId: sendOrderStatus
      message:
        $ref: '#/components/messages/OrderStatus'
components:
  messages:
    OrderStatus:
      name: OrderStatus
      payload:
        type: object

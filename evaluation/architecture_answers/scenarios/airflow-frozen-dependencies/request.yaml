scenario: airflow-frozen-dependencies
description: >
  I3 spec section 38/section 38.1. Derived from the frozen v0.3 Apache Airflow capture (see
  SOURCE.md) - airflow-apiserver is known but has zero supported outgoing CALLS/SENDS in the
  bounded frozen scope, so this is ANSWERED with empty claims, distinguished from an unknown
  service.

request:
  service_id: service:airflow-apiserver
  observation:
    environment: test
    window:
      start: "2026-08-26T00:00:00Z"
      end: "2026-08-27T00:00:00Z"
